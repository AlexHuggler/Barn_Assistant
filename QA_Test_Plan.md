# EquineLog iOS App — Comprehensive Manual Test Plan

**Version:** 1.0
**Date:** 2026-02-26
**Prepared by:** QA Engineering
**App:** EquineLog — Barn Management for iOS
**Target:** App Store Submission

---

## Table of Contents

1. [Test Plan Summary](#test-plan-summary)
2. [Section 1: Happy Path Test Cases](#section-1-happy-path-test-cases) (HP-001 to HP-042)
3. [Section 2: Negative Testing & Edge Cases](#section-2-negative-testing--edge-cases) (NE-001 to NE-038)
4. [Section 3: iOS & Apple Review Specific](#section-3-ios--apple-review-specific) (AR-001 to AR-030)
5. [Test Environment Requirements](#test-environment-requirements)
6. [Apple Review Rejection Risk Areas](#apple-review-rejection-risk-areas)

---

## Test Plan Summary

| Category | Test Case Range | Count |
|----------|----------------|-------|
| Happy Path | HP-001 to HP-042 | 42 |
| Negative Testing & Edge Cases | NE-001 to NE-038 | 38 |
| iOS & Apple Review Specific | AR-001 to AR-030 | 30 |
| **Total** | | **110** |

---

# Section 1: Happy Path Test Cases

This section covers the primary success-path scenarios for the EquineLog iOS app. Each test case validates core functionality under normal operating conditions with valid inputs and expected user behavior.

---

## 1.1 Onboarding Flow

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-001 | Onboarding | Complete full 6-step onboarding flow | 1. Fresh install of EquineLog (no prior data). 2. Device running iOS 17+. | 1. Launch the app for the first time. 2. Verify the Welcome screen appears with the app logo, tagline, and a "Get Started" button. 3. Tap "Get Started." 4. On the Barn Setup screen, enter a barn name (e.g., "Sunrise Stables") and tap "Next." 5. On the Use Case screen, select a use case option (e.g., "Private Barn Owner") and tap "Next." 6. On the Experience Level screen, select an experience level (e.g., "Intermediate") and tap "Next." 7. On the Feature Highlights screen, review the highlighted features carousel and tap "Next." 8. On the Quick Start screen, review the summary and tap "Finish." | 1. Each screen transitions with a smooth horizontal slide animation. 2. A progress indicator (dots or bar) updates at each step, reflecting the current step (1 of 6 through 6 of 6). 3. The barn name is persisted and visible in the app after onboarding. 4. The selected use case and experience level are stored in user preferences. 5. Upon tapping "Finish," the main app interface loads (Stable tab on iPhone, sidebar on iPad). 6. Each step is reachable via VoiceOver with accessibility labels such as "Welcome screen," "Barn Setup, step 2 of 6," etc. |
| HP-002 | Onboarding | Skip onboarding entirely | 1. Fresh install of EquineLog. 2. Device running iOS 17+. | 1. Launch the app for the first time. 2. On the Welcome screen, locate and tap the "Skip" button (top-right corner or bottom link). 3. Observe the transition to the main app interface. | 1. The "Skip" button is visible and has the accessibility label "Skip onboarding." 2. Tapping "Skip" dismisses the entire onboarding flow immediately. 3. The main app interface loads with default settings applied (no barn name set, default use case and experience level). 4. No guided tour overlay appears after skipping. 5. The transition uses a fade animation to the main interface. |
| HP-003 | Onboarding | Guided tour overlay appears after onboarding completion | 1. Onboarding flow completed (HP-001 passed). 2. App has loaded the main interface for the first time. | 1. Complete the full onboarding flow per HP-001. 2. After the main interface loads, observe the guided tour overlay. 3. Verify the first tooltip highlights a UI element relevant to the selected use case (e.g., for "Private Barn Owner," the first tooltip highlights the Feed Board). 4. Tap the "Next" button on the tooltip to advance through each tour step. 5. Complete all tour steps by tapping "Done" on the final tooltip. | 1. The guided tour overlay appears within 1 second of main interface load. 2. A semi-transparent backdrop dims the background behind each tooltip. 3. The highlighted UI element is visually emphasized with a spotlight cutout. 4. Tour steps are specific to the use case selected during onboarding (different use cases produce different tour step sequences). 5. Each tooltip includes a "Next" button and a step counter (e.g., "1 of 4"). 6. Upon completing the tour, the overlay dismisses with a fade-out animation and the app is fully interactive. 7. A light haptic tap (UIImpactFeedbackGenerator, style: .light) fires on each "Next" tap. |
| HP-004 | Onboarding | Dismiss guided tour with "I'll figure it out" | 1. Onboarding flow completed. 2. Guided tour overlay is displayed. | 1. Complete the full onboarding flow per HP-001. 2. When the guided tour overlay appears, locate the "I'll figure it out" link at the bottom of the first tooltip. 3. Tap "I'll figure it out." 4. Observe the tour dismissal. | 1. The "I'll figure it out" link is visible on every tour tooltip and has the accessibility label "Dismiss guided tour." 2. Tapping the link immediately dismisses the entire guided tour overlay. 3. The overlay fades out smoothly (approximately 0.3 second animation). 4. The main app interface becomes fully interactive. 5. The tour dismissal state is persisted so the tour does not reappear on subsequent launches. |
| HP-005 | Onboarding | Default feed templates seeded during onboarding | 1. Fresh install of EquineLog. 2. Device running iOS 17+. | 1. Complete the full onboarding flow (or skip it). 2. Navigate to the Feed Templates section (Settings tab or via Horse Profile). 3. Review the list of available feed templates. | 1. Exactly 5 default feed templates are present: "Hay Only," "Grain + Hay," "Light Work," "Performance," and "Senior." 2. Each template displays its name, a brief description, and a usage count of 0. 3. Templates are listed in the order above. 4. Each template row has an accessibility label in the format "[Template Name], feed template, used 0 times." 5. No duplicate templates exist. 6. Templates are persisted in SwiftData and survive app restart. |

---

## 1.2 Feed Board

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-006 | Feed Board | View Feed Board with horses listed | 1. At least 2 horses added to the barn (e.g., "Luna" and "Thunder"). 2. Current day with no feedings recorded yet. | 1. Open the app and navigate to the Stable tab (Feed Board). 2. Observe the list of horses displayed. 3. Verify each horse row shows AM and PM feed toggle indicators. | 1. The Feed Board displays all horses in the barn in alphabetical order. 2. Each horse row shows the horse's name, avatar thumbnail, and two feed status indicators (AM and PM). 3. Both AM and PM indicators show an unfed state (empty circle or unchecked toggle). 4. The screen title reads "Feed Board" with the current date displayed below. 5. The filter defaults to "All." 6. Each horse row has the accessibility label "[Horse Name], AM not fed, PM not fed." |
| HP-007 | Feed Board | Toggle AM feed for a single horse | 1. At least 1 horse on the Feed Board. 2. AM feed not yet recorded for that horse. 3. Current time is before 2:00 PM (AM slot is active). | 1. On the Feed Board, locate the horse "Luna." 2. Tap the AM feed toggle for "Luna." 3. Observe the toggle state change and any feedback. | 1. The AM toggle transitions from unfed (empty circle) to fed (filled checkmark) with a smooth scale animation. 2. A success haptic fires (UINotificationFeedbackGenerator, type: .success). 3. The toggle's accessibility label updates to "Luna, AM fed." 4. The feed event is persisted in SwiftData with the current timestamp. 5. If the filter is set to "Needs Feeding," "Luna" remains visible if PM is still unfed; if set to "Fed" and PM is unfed, "Luna" does not appear in the "Fed" filter. |
| HP-008 | Feed Board | Toggle PM feed for a single horse | 1. At least 1 horse on the Feed Board. 2. PM feed not yet recorded for that horse. 3. Current time is 2:00 PM or later (PM slot is active). | 1. On the Feed Board, locate the horse "Thunder." 2. Tap the PM feed toggle for "Thunder." 3. Observe the toggle state change and any feedback. | 1. The PM toggle transitions from unfed to fed with a smooth scale animation. 2. A success haptic fires (UINotificationFeedbackGenerator, type: .success). 3. The toggle's accessibility label updates to "Thunder, PM fed." 4. The PM feed event is persisted in SwiftData with the current timestamp. 5. The horse row updates to reflect the new feeding status immediately. |
| HP-009 | Feed Board | Mark All Fed button feeds all horses for active slot | 1. At least 3 horses on the Feed Board. 2. None of them fed for the current active slot (AM if before 2 PM, PM if after 2 PM). | 1. On the Feed Board, locate and tap the "Mark All Fed" button. 2. Observe the state change across all horse rows. 3. Observe any banner or confirmation. | 1. All horse rows update their active-slot toggle to the fed state simultaneously with a staggered animation (each row animates 50ms after the previous). 2. A celebration banner appears at the top of the screen reading "All horses fed!" with a confetti or checkmark animation. 3. The celebration banner auto-dismisses after 3 seconds. 4. A success haptic fires once when the action completes. 5. The "Mark All Fed" button becomes disabled or hidden after all horses are marked fed. 6. Each feed event is persisted in SwiftData with the current timestamp. 7. The button has the accessibility label "Mark all horses as fed." |
| HP-010 | Feed Board | Undo banner appears after Mark All Fed | 1. "Mark All Fed" has just been triggered (HP-009). 2. Celebration banner or undo banner is visible. | 1. After tapping "Mark All Fed," observe the undo banner that appears. 2. Verify the banner displays "Undo" action text. 3. Wait and observe the banner's auto-dismiss behavior. | 1. An undo banner appears at the bottom or top of the Feed Board within 0.5 seconds of the "Mark All Fed" action. 2. The banner reads "All marked as fed" with a tappable "Undo" button. 3. The banner has the accessibility label "Undo mark all fed, dismisses in 4 seconds." 4. The banner remains visible for exactly 4 seconds before auto-dismissing with a slide-out animation. 5. While the banner is visible, the feed events are committed but reversible. |
| HP-011 | Feed Board | Tap Undo on the undo banner to revert Mark All Fed | 1. "Mark All Fed" triggered and undo banner is visible (HP-010). 2. Less than 4 seconds have elapsed since the banner appeared. | 1. Tap the "Undo" button on the undo banner before it auto-dismisses. 2. Observe the state of all horse feed toggles. | 1. All horse feed toggles revert to their pre-"Mark All Fed" state (unfed for those that were unfed before). 2. The undo banner dismisses immediately upon tapping "Undo." 3. A light haptic fires (UIImpactFeedbackGenerator, style: .light) on undo. 4. The "Mark All Fed" button reappears or re-enables. 5. The reverted state is persisted in SwiftData (feed events are deleted). 6. A brief confirmation toast reading "Feeding undone" appears for 2 seconds. |
| HP-012 | Feed Board | Filter horses by "Needs Feeding" | 1. At least 3 horses on the Feed Board. 2. At least 1 horse is fully fed (both AM and PM), and at least 1 horse has not been fed for the current slot. | 1. On the Feed Board, locate the filter control (segmented control or picker with options: All, Needs Feeding, Fed). 2. Tap "Needs Feeding." 3. Observe the filtered list. | 1. The list updates to show only horses that have not been fed for the current active slot (AM before 2 PM, PM at/after 2 PM). 2. Horses that are fully fed for the active slot are hidden from the list. 3. The filter selection is visually highlighted (bold text or filled background on the "Needs Feeding" segment). 4. The transition uses a fade or list re-layout animation. 5. The count of displayed horses matches the number of unfed horses. 6. The filter segment has the accessibility label "Needs Feeding filter, [N] horses." |
| HP-013 | Feed Board | Filter horses by "Fed" | 1. At least 3 horses on the Feed Board. 2. At least 1 horse is fed for the current active slot. | 1. On the Feed Board, tap the "Fed" filter option. 2. Observe the filtered list. | 1. The list updates to show only horses that have been fed for the current active slot. 2. Unfed horses are hidden. 3. The "Fed" filter segment is visually selected. 4. If no horses are fed, an empty state message is displayed (e.g., "No horses fed yet"). 5. The filter segment has the accessibility label "Fed filter, [N] horses." |
| HP-014 | Feed Board | Search for a horse by name on the Feed Board | 1. At least 3 horses on the Feed Board with distinct names (e.g., "Luna," "Thunder," "Starlight"). | 1. On the Feed Board, tap the search bar at the top of the list. 2. Type "Lun" into the search field. 3. Observe the filtered results as you type. 4. Clear the search field. | 1. The search bar activates with keyboard presentation and has the placeholder text "Search horses." 2. As "Lun" is typed, the list filters in real-time to show only "Luna." 3. Other horses ("Thunder," "Starlight") are hidden while the search term is active. 4. The search is case-insensitive. 5. Clearing the search field restores the full list (respecting any active filter). 6. The search bar has the accessibility label "Search horses." |
| HP-015 | Feed Board | Swipe to delete a horse from Feed Board | 1. At least 2 horses on the Feed Board. | 1. On the Feed Board, perform a full left swipe on the horse "Thunder." 2. Observe the delete action. 3. Confirm deletion if a confirmation dialog appears. | 1. A left swipe reveals a red "Delete" action button. 2. A full swipe or tap on "Delete" triggers a confirmation alert: "Remove Thunder from the Feed Board?" with "Cancel" and "Delete" buttons. 3. Tapping "Delete" removes the horse row with a slide-left animation. 4. A warning haptic fires (UINotificationFeedbackGenerator, type: .warning) on deletion. 5. The horse is removed from the Feed Board list in SwiftData. 6. The deletion is reflected immediately in the list count and any active filter counts. |
| HP-016 | Feed Board | Swipe to log event from Feed Board | 1. At least 1 horse on the Feed Board. | 1. On the Feed Board, perform a partial right swipe on the horse "Luna." 2. Observe the revealed action. 3. Tap the "Log Event" action button. | 1. A right swipe reveals a teal or blue "Log Event" action button with an icon. 2. Tapping "Log Event" navigates to the Add Health Event screen pre-populated with "Luna" as the selected horse. 3. The transition uses a standard navigation push animation. 4. A light haptic fires on swipe reveal. 5. The swipe action has the accessibility label "Log event for Luna." |
| HP-017 | Feed Board | Daily auto-reset clears all feed statuses at new day | 1. At least 2 horses with AM and/or PM feeds recorded for the current day. 2. Ability to advance the device clock to the next calendar day (or test via date simulation). | 1. Record AM and PM feeds for at least 2 horses. 2. Advance the device date to the next calendar day (e.g., from Feb 26 to Feb 27). 3. Reopen the app or bring it to foreground. 4. Navigate to the Feed Board. | 1. All AM and PM feed toggles are reset to the unfed state for the new day. 2. The Feed Board header date updates to the new calendar day. 3. Historical feed data from the previous day remains in SwiftData (queryable via Health Timeline or Horse Profile). 4. The "Mark All Fed" button is re-enabled. 5. Any active filter resets to "All." 6. No residual undo banners from the previous day are displayed. |
| HP-018 | Feed Board | AM/PM slot cutoff at 2 PM | 1. At least 1 horse on the Feed Board. 2. Device time set to 1:59 PM. | 1. At 1:59 PM, open the Feed Board and note which slot is active (AM toggle should be interactive/prominent). 2. Wait for the device clock to reach 2:00 PM (or advance the clock). 3. Observe the Feed Board update. | 1. Before 2:00 PM, the AM toggle is the primary interactive element with a visual emphasis (bold border or highlight), and the PM toggle is dimmed or secondary. 2. At or after 2:00 PM, the PM toggle becomes the primary interactive element, and the AM toggle becomes secondary (still viewable but no longer emphasized). 3. The transition occurs without requiring a manual refresh. 4. The active slot label updates (e.g., "Morning Feed" changes to "Evening Feed" in the header or subheader). 5. The "Mark All Fed" button context switches to the PM slot. |
| HP-019 | Feed Board | Celebration banner when all horses are fed for both slots | 1. At least 2 horses on the Feed Board. 2. All horses have been fed for the AM slot. 3. Current time is 2:00 PM or later (PM slot active). | 1. Feed all remaining horses for the PM slot, either individually or via "Mark All Fed." 2. Observe the celebration banner after the last horse is fed. | 1. A celebration banner appears prominently when the final horse's PM feed is toggled. 2. The banner displays a message such as "All horses fed for the day!" with a celebratory animation (e.g., confetti particles or a checkmark burst). 3. The banner auto-dismisses after 4 seconds. 4. A success haptic fires (UINotificationFeedbackGenerator, type: .success). 5. The celebration banner has the accessibility announcement "Congratulations, all horses have been fed for the day." |

---

## 1.3 Health Timeline

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-020 | Health Timeline | View Health Timeline with existing events | 1. At least 2 horses in the barn. 2. At least 3 health events recorded across different types (e.g., 1 Farrier, 1 Vet, 1 Deworming). | 1. Navigate to the Health tab. 2. Observe the Health Timeline screen. 3. Scroll through the list of events. | 1. The Health Timeline displays events in reverse chronological order (most recent first). 2. Each event row shows: event type icon, horse name, date, provider name (if recorded), and cost (if recorded). 3. Events are grouped by date or displayed in a continuous timeline with date separators. 4. The screen title reads "Health Timeline." 5. Each event row has an accessibility label in the format "[Event Type] for [Horse Name] on [Date]." 6. Overdue events display a red or orange overdue indicator badge. |
| HP-021 | Health Timeline | Add a Farrier event with auto-calculated due date | 1. At least 1 horse in the barn (e.g., "Luna"). | 1. On the Health Timeline, tap the "+" or "Add Event" button. 2. Select event type "Farrier." 3. Select horse "Luna." 4. Enter the date as today's date. 5. Enter provider name "John Smith" in the provider field. 6. Enter cost "$85.00." 7. Tap "Save." | 1. The event type picker offers options: Farrier, Vet, Deworming, Dental. 2. Selecting "Farrier" displays the Farrier-specific form fields. 3. The "Next Due" date is auto-calculated as 7 weeks (49 days) from the entered date and displayed in a read-only field. 4. The provider field shows auto-suggest results if "John Smith" has been used before. 5. Tapping "Save" dismisses the form with a slide-down or pop animation. 6. A success haptic fires on save. 7. The new event appears at the top of the Health Timeline. 8. The event is persisted in SwiftData and the "Next Due" date is stored. 9. The "Save" button has the accessibility label "Save health event." |
| HP-022 | Health Timeline | Add a Vet event with auto-calculated due date | 1. At least 1 horse in the barn. | 1. Tap "Add Event" on the Health Timeline. 2. Select event type "Vet." 3. Select a horse. 4. Enter today's date. 5. Enter provider name "Dr. Adams." 6. Enter cost "$250.00." 7. Add optional notes "Annual wellness check." 8. Tap "Save." | 1. Selecting "Vet" auto-calculates the next due date as 6 months from the entered date. 2. The auto-calculated due date is displayed and correct (e.g., if entered date is Feb 26, 2026, next due is Aug 26, 2026). 3. The notes field accepts free-form text. 4. The event is saved and appears on the Health Timeline with all entered details. 5. A success haptic fires on save. 6. The provider "Dr. Adams" is stored for future auto-suggest. |
| HP-023 | Health Timeline | Add a Deworming event with auto-calculated due date | 1. At least 1 horse in the barn. | 1. Tap "Add Event" on the Health Timeline. 2. Select event type "Deworming." 3. Select a horse. 4. Enter today's date. 5. Enter cost "$15.00." 6. Tap "Save." | 1. Selecting "Deworming" auto-calculates the next due date as 8 weeks (56 days) from the entered date. 2. The auto-calculated due date is displayed and correct. 3. The provider field is optional and can be left blank. 4. The event is saved and appears on the Health Timeline. 5. A success haptic fires on save. |
| HP-024 | Health Timeline | Add a Dental event with auto-calculated due date | 1. At least 1 horse in the barn. | 1. Tap "Add Event" on the Health Timeline. 2. Select event type "Dental." 3. Select a horse. 4. Enter today's date. 5. Enter provider "Dr. Williams." 6. Enter cost "$175.00." 7. Tap "Save." | 1. Selecting "Dental" auto-calculates the next due date as 1 year (365 days) from the entered date. 2. The auto-calculated due date is displayed and correct (e.g., Feb 26, 2026 yields Feb 26, 2027). 3. The event is saved and appears on the Health Timeline. 4. A success haptic fires on save. |
| HP-025 | Health Timeline | Edit an existing health event | 1. At least 1 health event exists on the Health Timeline. | 1. On the Health Timeline, tap an existing event row (e.g., a Farrier event for "Luna"). 2. The event detail or edit screen opens. 3. Modify the cost from "$85.00" to "$95.00." 4. Modify the provider from "John Smith" to "John Smith Jr." 5. Tap "Save." | 1. Tapping an event row navigates to the event detail/edit screen with all fields pre-populated. 2. All fields are editable (date, provider, cost, notes). 3. Changing the date recalculates the "Next Due" date automatically. 4. Tapping "Save" persists the changes in SwiftData. 5. The updated event row on the Health Timeline reflects the new cost and provider. 6. A success haptic fires on save. 7. The navigation returns to the Health Timeline after saving. |
| HP-026 | Health Timeline | Filter events by type | 1. At least 4 health events across different types (Farrier, Vet, Deworming, Dental). | 1. On the Health Timeline, locate the filter control (segmented control or dropdown). 2. Select "Farrier" from the type filter. 3. Observe the filtered list. 4. Select "All" to reset the filter. | 1. The filter control displays options for each event type plus "All." 2. Selecting "Farrier" shows only Farrier-type events; all other types are hidden. 3. The list updates with a fade or re-layout animation. 4. The count of displayed events matches the number of Farrier events in the data. 5. Selecting "All" restores the complete event list. 6. The filter has the accessibility label "Filter by event type." |
| HP-027 | Health Timeline | Filter events by horse | 1. At least 2 horses with health events (e.g., "Luna" has 2 events, "Thunder" has 1 event). | 1. On the Health Timeline, locate the horse filter control. 2. Select "Luna" from the horse filter. 3. Observe the filtered list. 4. Select "All Horses" to reset. | 1. The horse filter displays all horses in the barn plus an "All Horses" option. 2. Selecting "Luna" shows only events associated with "Luna." 3. Events for "Thunder" and other horses are hidden. 4. The filter can be combined with the type filter (e.g., "Luna" + "Farrier" shows only Luna's farrier events). 5. Selecting "All Horses" restores the full event list. 6. The filter has the accessibility label "Filter by horse." |

---

## 1.4 Weather Dashboard

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-028 | Weather Dashboard | View current weather conditions | 1. Device has an active internet connection. 2. Location Services enabled and authorized for EquineLog. 3. WeatherKit entitlement is configured. | 1. Navigate to the Weather tab. 2. Observe the Weather Dashboard loading state. 3. Once loaded, review the displayed weather data. | 1. A loading indicator (spinner or skeleton view) appears briefly while weather data is fetched. 2. The dashboard displays: current temperature (in user's preferred unit), humidity percentage, wind speed and direction. 3. The location name or barn location is displayed in the header. 4. A "Last updated" timestamp is shown (e.g., "Updated 2 min ago"). 5. Data values are formatted with appropriate units (e.g., "45 degrees F," "65% humidity," "10 mph NW"). 6. The screen title reads "Weather." 7. Each data element has an accessibility label (e.g., "Current temperature, 45 degrees Fahrenheit"). |
| HP-029 | Weather Dashboard | Blanketing recommendations based on temperature and clipped status | 1. Weather data is loaded on the Weather Dashboard. 2. At least 1 horse exists with a known clipped status. | 1. On the Weather Dashboard, scroll to the Blanketing Recommendations section. 2. Review the recommendation displayed for the current temperature. 3. If possible, verify the recommendation matches one of the 6 tiers. | 1. The Blanketing Recommendations section displays a recommendation based on the current temperature and horse clipped status. 2. The 6 tiers are applied correctly: (a) Above 60 degrees F: No blanket needed, (b) 50-60 degrees F: Light sheet for clipped horses, (c) 40-50 degrees F: Medium-weight blanket for clipped / light sheet for unclipped, (d) 30-40 degrees F: Heavy blanket for clipped / medium blanket for unclipped, (e) 20-30 degrees F: Heavy blanket with neck cover for clipped / heavy blanket for unclipped, (f) Below 20 degrees F: Heaviest blanket with full coverage for all horses. 3. If multiple horses have different clipped statuses, recommendations are shown per horse or grouped by clipped/unclipped. 4. The recommendation section has the accessibility label "Blanketing recommendation: [recommendation text]." |
| HP-030 | Weather Dashboard | Weather data uses 15-minute cache | 1. Weather Dashboard has been loaded at least once within the last 15 minutes. | 1. Navigate away from the Weather tab. 2. Return to the Weather tab within 15 minutes. 3. Observe the data load behavior. 4. Note the "Last updated" timestamp. | 1. The Weather Dashboard loads instantly without a network request (no loading spinner). 2. The "Last updated" timestamp reflects the original fetch time (not the current time). 3. The displayed weather data is identical to the previously fetched data. 4. No visible loading indicator appears. 5. If 15 minutes have elapsed since the last fetch, a fresh network request is made (loading indicator appears briefly), and the "Last updated" timestamp updates. |
| HP-031 | Weather Dashboard | Force refresh weather data | 1. Weather Dashboard is displayed with cached data. | 1. On the Weather Dashboard, perform a pull-to-refresh gesture (or tap a dedicated "Refresh" button). 2. Observe the loading state and data update. | 1. A pull-to-refresh indicator (spinning arrows) appears at the top of the dashboard. 2. A network request is made to WeatherKit regardless of cache age. 3. The loading indicator is displayed during the fetch. 4. Upon completion, the weather data updates with the latest values. 5. The "Last updated" timestamp updates to the current time. 6. A light haptic fires when the refresh triggers (UIImpactFeedbackGenerator, style: .medium). 7. If the network request fails, an error banner is displayed and the previous cached data remains visible. 8. The refresh control has the accessibility label "Refresh weather data." |

---

## 1.5 Horse Profile

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-032 | Horse Profile | View Horse Profile with avatar, feed schedule, and quick stats | 1. At least 1 horse in the barn with a profile photo, feed schedule, and at least 1 health event. | 1. Navigate to the Stable tab. 2. Tap on a horse (e.g., "Luna") to open the Horse Profile. 3. Review the profile screen layout. | 1. The Horse Profile screen displays: a circular avatar image at the top (or a placeholder silhouette if no photo), the horse's name as the screen title, and a feed schedule card showing the assigned feed template and feeding times. 2. A Quick Stats section shows key metrics (e.g., age, breed, last farrier date, last vet date). 3. A Health History section lists recent health events in reverse chronological order. 4. The avatar has the accessibility label "Profile photo for Luna." 5. The feed schedule card has the accessibility label "Feed schedule: [Template Name]." 6. All sections are scrollable in a single vertical scroll view. |
| HP-033 | Horse Profile | Edit feed schedule from Horse Profile | 1. A horse profile exists with an assigned feed template. | 1. Open the Horse Profile for "Luna." 2. Tap the "Edit Feed" button on the feed schedule card. 3. On the edit screen, change the feed template from "Hay Only" to "Grain + Hay." 4. Tap "Save." | 1. Tapping "Edit Feed" navigates to a feed schedule editing screen. 2. The current template is pre-selected. 3. All available feed templates are listed for selection. 4. Selecting "Grain + Hay" highlights it visually. 5. Tapping "Save" persists the change and returns to the Horse Profile. 6. The feed schedule card updates to display "Grain + Hay." 7. A success haptic fires on save. 8. The usage count for "Grain + Hay" increments by 1, and "Hay Only" decrements by 1 in the feed templates data. |
| HP-034 | Horse Profile | Generate PDF report (30-day) | 1. A horse exists with at least 5 health events and 14 days of feeding data. | 1. Open the Horse Profile for "Luna." 2. Locate and tap the "Generate Report" button. 3. Wait for the PDF to generate. 4. Observe the generated PDF preview. | 1. Tapping "Generate Report" shows a loading indicator with the text "Generating report..." 2. The PDF is generated within 5 seconds. 3. A PDF preview screen appears showing a formatted 30-day report. 4. The report includes: horse name, date range (last 30 days), feeding compliance summary, health events in the period, cost summary, and any overdue items. 5. The report header includes the barn name and generation date. 6. The PDF is visually formatted with sections, tables, and the horse's avatar. 7. A success haptic fires when generation completes. 8. The "Generate Report" button has the accessibility label "Generate 30-day PDF report for Luna." |
| HP-035 | Horse Profile | Share PDF report via share sheet | 1. A PDF report has been generated (HP-034 completed). | 1. On the PDF preview screen, tap the "Share" button (share icon). 2. Observe the iOS share sheet presentation. 3. Select a share destination (e.g., "Save to Files"). | 1. Tapping the "Share" button presents the standard iOS UIActivityViewController share sheet. 2. The share sheet displays relevant sharing options (AirDrop, Messages, Mail, Save to Files, etc.). 3. The shared item is a properly formatted PDF file with the filename format "Luna_Report_2026-02-26.pdf." 4. Selecting "Save to Files" saves the PDF successfully to the chosen location. 5. The share button has the accessibility label "Share report." 6. Dismissing the share sheet returns the user to the PDF preview screen. |
| HP-036 | Horse Profile | View analytics dashboard with cost trend, compliance, and projected annual | 1. A horse exists with at least 30 days of data including feeding records and health events with costs. | 1. Open the Horse Profile for "Luna." 2. Scroll down to or tap on the "Analytics" section. 3. Review the analytics dashboard. | 1. The Analytics section displays three sub-sections: Cost Trend, Compliance, and Projected Annual Cost. 2. Cost Trend shows a line or bar chart plotting health-related costs over time (minimum 30-day window). 3. Compliance shows a feeding compliance percentage (e.g., "92% fed on time") with a circular progress indicator or bar graph. 4. Projected Annual Cost displays an estimated yearly cost extrapolated from existing data (e.g., "$2,400/year"). 5. Each chart is interactive (tappable data points show tooltips with exact values). 6. Charts use the accessibility trait "isAccessibilityElement" with descriptive summaries (e.g., "Cost trend chart, showing $850 total over 30 days"). 7. Data updates in real-time as new events are added. |

---

## 1.6 Feed Templates

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-037 | Feed Templates | Apply a default feed template to a horse | 1. At least 1 horse exists. 2. Default feed templates have been seeded (HP-005). | 1. Navigate to the Horse Profile for "Thunder." 2. Tap "Edit Feed" on the feed schedule card. 3. Select the "Performance" template from the list. 4. Tap "Apply" or "Save." | 1. The "Performance" template is highlighted when selected. 2. A preview of the template's feed details (grain types, hay amounts, supplements) is shown before applying. 3. Tapping "Apply" assigns the template to "Thunder" and returns to the Horse Profile. 4. The feed schedule card on Thunder's profile updates to display the "Performance" template details. 5. A success haptic fires on apply. 6. The usage count for "Performance" increments by 1 in the feed templates list. 7. The "Apply" button has the accessibility label "Apply Performance template to Thunder." |
| HP-038 | Feed Templates | Save a custom feed template | 1. User is on the Feed Templates management screen or creating a new template. | 1. Navigate to the Feed Templates section (via Settings or Horse Profile). 2. Tap "Create Template" or "+" button. 3. Enter template name "Custom Senior Blend." 4. Configure feed components (e.g., 2 flakes hay, 1 scoop senior grain, 1 scoop joint supplement). 5. Tap "Save." | 1. The create template form includes fields for: name, feed components (with quantity and type), and optional notes. 2. Each feed component can be added via an "Add Component" button. 3. The template name is validated (non-empty, no duplicates). 4. Tapping "Save" persists the custom template in SwiftData. 5. The new "Custom Senior Blend" template appears in the templates list alongside the 5 defaults. 6. The custom template has a usage count of 0. 7. A success haptic fires on save. 8. The "Save" button has the accessibility label "Save custom feed template." |
| HP-039 | Feed Templates | Delete a custom feed template | 1. At least 1 custom feed template exists (HP-038 completed). 2. The template is not currently assigned to any horse (or the test handles reassignment). | 1. Navigate to the Feed Templates list. 2. Perform a left swipe on the "Custom Senior Blend" template. 3. Tap the "Delete" button. 4. Confirm deletion if prompted. | 1. A left swipe reveals a red "Delete" button. 2. Tapping "Delete" shows a confirmation alert: "Delete 'Custom Senior Blend'? This cannot be undone." with "Cancel" and "Delete" options. 3. Tapping "Delete" removes the template from the list with a slide-out animation. 4. A warning haptic fires on deletion (UINotificationFeedbackGenerator, type: .warning). 5. The template is removed from SwiftData. 6. The 5 default templates remain unaffected and are not deletable (swipe-to-delete is disabled for defaults, or "Delete" is not shown). |
| HP-040 | Feed Templates | Usage count tracking across template assignments | 1. At least 2 horses and at least 2 feed templates exist. | 1. Navigate to the Feed Templates list and note the usage count for "Hay Only" (e.g., 0). 2. Assign "Hay Only" to horse "Luna" via Edit Feed. 3. Assign "Hay Only" to horse "Thunder" via Edit Feed. 4. Return to the Feed Templates list. 5. Change "Thunder" to "Grain + Hay" and return to the templates list. | 1. After step 2, "Hay Only" usage count is 1. 2. After step 3, "Hay Only" usage count is 2. 3. After step 5, "Hay Only" usage count decrements to 1, and "Grain + Hay" usage count increments to 1. 4. Usage counts update in real-time when returning to the templates list. 5. Each template row displays the count in the format "Used by [N] horse(s)." 6. The usage count has the accessibility label "[Template Name], used by [N] horses." |

---

## 1.7 Settings

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| HP-041 | Settings | Configure notification preferences with master toggle and per-moment toggles | 1. App is installed with notification permissions granted. 2. At least 1 horse exists in the barn. | 1. Navigate to the Settings tab. 2. Locate the "Notifications" section. 3. Verify the master notification toggle is ON. 4. Review the per-moment toggles (e.g., "AM Feed Reminder," "PM Feed Reminder," "Health Event Due," "Weather Alert"). 5. Toggle "PM Feed Reminder" OFF. 6. Toggle "PM Feed Reminder" back ON. 7. Attempt to enable a 5th notification moment if 4 are already enabled. | 1. The master toggle controls all notification delivery; turning it OFF disables all per-moment toggles (grayed out). 2. Turning the master toggle ON re-enables the per-moment toggles to their previous states. 3. Each per-moment toggle can be independently turned ON or OFF. 4. Toggling a notification moment ON/OFF produces a light haptic (UIImpactFeedbackGenerator, style: .light). 5. A maximum of 4 active notification moments is enforced; enabling a 5th shows an inline message: "Maximum of 4 daily notifications reached. Disable another to enable this one." 6. Notification preferences are persisted and survive app restart. 7. The master toggle has the accessibility label "Master notification toggle, currently [on/off]." 8. Each per-moment toggle has the accessibility label "[Moment Name] notification, currently [on/off]." |
| HP-042 | Settings | Replay onboarding, guided tour, and tips | 1. Onboarding has been completed previously. 2. Guided tour has been completed or dismissed. | 1. Navigate to the Settings tab. 2. Locate the "Help & Support" or "Replay" section. 3. Tap "Replay Onboarding." 4. Complete or skip onboarding. 5. Return to Settings. 6. Tap "Replay Guided Tour." 7. Complete or dismiss the guided tour. 8. Return to Settings. 9. Tap "Replay Tips." | 1. "Replay Onboarding" launches the full 6-step onboarding flow from step 1 (Welcome), identical to HP-001 behavior. 2. Completing or skipping onboarding returns the user to the Settings screen (not the main Stable tab). 3. "Replay Guided Tour" launches the guided tour overlay from step 1, identical to HP-003 behavior but triggered from Settings. 4. Completing or dismissing the guided tour returns the user to Settings. 5. "Replay Tips" re-enables contextual tip popovers throughout the app (tips that were previously dismissed reappear on their respective screens). 6. Each replay button has an accessibility label: "Replay onboarding," "Replay guided tour," "Replay tips." 7. A light haptic fires when each replay option is tapped. 8. None of the replay actions reset user data, preferences, or horse/event records. |


---

# Section 2: Negative Testing & Edge Cases

This section covers negative testing and edge case scenarios for the EquineLog iOS app. Each test case validates that the app correctly handles invalid input, boundary conditions, error states, and unusual user workflows.

---

## 2.1 Form Validation (Horse Creation)

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-001 | Form Validation | Empty horse name is rejected | App is open on Feed Board | 1. Tap the "+" button in the top-right toolbar to open the Add Horse sheet.<br>2. Leave the "Name" field completely empty.<br>3. Enter a valid owner name (e.g., "Jane Smith") in the "Owner Name" field.<br>4. Tap the "Save" button in the toolbar. | The Save button should be disabled (grayed out) because `isFormValid` returns false. The `hasAttemptedSave` flag is set to true, causing a red exclamationmark.circle.fill icon to appear next to the Name field. The validation message "Horse name is required." appears in red caption text below the Name field. An error haptic fires. The horse is NOT saved. |
| NE-002 | Form Validation | Single-character horse name is rejected (below 2-char minimum) | App is open on Feed Board | 1. Tap the "+" button to open the Add Horse sheet.<br>2. Type "A" in the "Name" field (exactly 1 character).<br>3. Enter "Jane Smith" in the "Owner Name" field.<br>4. Tap anywhere else to trigger field-touched validation.<br>5. Attempt to tap "Save". | The `nameFieldTouched` flag becomes true after typing. The `FormValidation.validateHorseName` function trims the input and finds `trimmed.count < 2`. A red exclamationmark.circle.fill icon appears beside the Name field. The validation message "Name must be at least 2 characters." appears in red caption text. The Save button remains disabled. The horse is NOT saved. |
| NE-003 | Form Validation | Horse name exceeding 40 characters is rejected | App is open on Feed Board | 1. Tap the "+" button to open the Add Horse sheet.<br>2. In the "Name" field, type a string of exactly 41 characters (e.g., "ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789012345").<br>3. Enter "Jane Smith" in the "Owner Name" field.<br>4. Observe the real-time validation indicator. | The `FormValidation.validateHorseName` function evaluates `trimmed.count > 40` as true. A red exclamationmark.circle.fill icon appears next to the Name field. The validation message "Name must be under 40 characters." appears in red caption text below the field. The Save button is disabled. The horse is NOT saved. |
| NE-004 | Form Validation | Horse name at exact boundary (2 chars and 40 chars) is accepted | App is open on Feed Board | 1. Tap the "+" button to open the Add Horse sheet.<br>2. Type "Al" in the "Name" field (exactly 2 characters).<br>3. Enter "Jane Smith" in the "Owner Name" field.<br>4. Verify the validation indicator shows a green checkmark.circle.fill.<br>5. Tap "Save" and confirm the horse is saved.<br>6. Repeat the process: tap "+", enter a name of exactly 40 characters (e.g., "ABCDEFGHIJKLMNOPQRSTUVWXYZ12345678901234"), enter an owner name, and tap "Save". | For the 2-character name: a green checkmark.circle.fill icon appears next to the Name field, no error message is shown, the Save button is enabled, and the horse is saved successfully with a toast "[name] added!". For the 40-character name: the same green checkmark appears, no error message is shown, the Save button is enabled, and the horse is saved successfully. Both boundary values pass `FormValidation.validateHorseName`. |
| NE-005 | Form Validation | Whitespace-only horse name is rejected (trimming behavior) | App is open on Feed Board | 1. Tap the "+" button to open the Add Horse sheet.<br>2. In the "Name" field, type several spaces ("     ").<br>3. Enter "Jane Smith" in the "Owner Name" field.<br>4. Tap "Save". | The `FormValidation.validateHorseName` function calls `name.trimmingCharacters(in: .whitespacesAndNewlines)`, which reduces the spaces to an empty string. Since `trimmed.isEmpty` is true, the validation message "Horse name is required." appears in red caption text. The red exclamationmark.circle.fill icon is displayed. The Save button is disabled. The horse is NOT saved. Additionally, if a name like "  Bo  " is entered, it passes validation (trimmed to "Bo", which is 2 characters) and the horse is saved with the name trimmed to "Bo" via `name.trimmingCharacters(in: .whitespacesAndNewlines)` in `commitHorse()`. |
| NE-006 | Form Validation | Empty owner name is rejected | App is open on Feed Board | 1. Tap the "+" button to open the Add Horse sheet.<br>2. Enter "Thunder" in the "Name" field.<br>3. Leave the "Owner Name" field completely empty.<br>4. Tap "Save". | The Save button is disabled because `isFormValid` requires both `nameValidation.isValid` and `ownerValidation.isValid` to be true. The `FormValidation.validateOwnerName` function trims the input and finds it empty. After the save attempt (or once `ownerFieldTouched` is true), a red exclamationmark.circle.fill icon appears next to the Owner Name field. The validation message "Owner name is required." appears in red caption text. An error haptic fires. The horse is NOT saved. |

---

## 2.2 Health Event Validation

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-007 | Health Event Validation | Negative cost value is rejected | At least one horse exists in the app | 1. Navigate to the Health tab or open "Log Health Event" from any horse's profile or Feed Board quick-log action.<br>2. Select a horse if more than one exists.<br>3. Choose any event type (e.g., "Farrier").<br>4. In the "Cost" field (currency-formatted), enter a negative value such as "-50".<br>5. Tap "Save". | The `FormValidation.validateCost` function detects `cost < 0` and returns invalid. A red exclamationmark.circle.fill icon appears next to the Cost field. The validation message "Cost cannot be negative." appears in red caption text below the Cost field. An error haptic (`HapticManager.notification(.error)`) fires. The event is NOT saved. |
| NE-008 | Health Event Validation | Cost exceeding $99,999 is flagged | At least one horse exists in the app | 1. Open "Log Health Event".<br>2. Select a horse and choose event type "Vet".<br>3. In the "Cost" field, enter "100000" (one hundred thousand).<br>4. Tap outside the Cost field to trigger the `costFieldTouched` state.<br>5. Tap "Save". | The `FormValidation.validateCost` function detects `cost > 99_999` and returns invalid. A red exclamationmark.circle.fill icon appears next to the Cost field. The validation message "Cost seems unusually high. Please verify." appears in red caption text. An error haptic fires. The event is NOT saved. |
| NE-009 | Health Event Validation | Cost at exact boundary ($0 and $99,999) is accepted | At least one horse exists in the app | 1. Open "Log Health Event".<br>2. Select a horse, choose event type "Farrier".<br>3. Enter "0" in the Cost field.<br>4. Tap "Save" and verify the event is saved.<br>5. Open "Log Health Event" again.<br>6. Select the same horse, choose event type "Vet".<br>7. Enter "99999" in the Cost field.<br>8. Tap "Save". | For cost = $0.00: `FormValidation.validateCost` returns valid (0 is not < 0 and not > 99999). A green checkmark.circle.fill icon appears. The event is saved successfully with a toast "Event logged!". For cost = $99,999.00: `FormValidation.validateCost` returns valid (99999 is not > 99_999). A green checkmark.circle.fill icon appears. The event is saved successfully. Both boundary values are accepted. |
| NE-010 | Health Event Validation | Nil (empty) cost is accepted as optional field | At least one horse exists in the app | 1. Open "Log Health Event".<br>2. Select a horse and choose event type "Dental".<br>3. Leave the "Cost" field completely empty (do not type any value).<br>4. Fill in other fields as desired.<br>5. Tap "Save". | The `cost` state variable is `Double?` and defaults to `nil`. The `FormValidation.validateCost` function checks `guard let cost else { return .valid }` -- since cost is nil, it returns `.valid` immediately. No validation error appears for the cost field. The event is saved successfully with `cost: nil` on the `HealthEvent` object. A toast "Event logged!" appears. |
| NE-011 | Health Event Validation | Next due date before event date is rejected (manual date mode) | At least one horse exists in the app | 1. Open "Log Health Event".<br>2. Select a horse and choose event type "Farrier".<br>3. Set the event "Date" to today's date.<br>4. Toggle "Auto-calculate" OFF to reveal the "Custom Date" picker.<br>5. Set "Custom Date" to a date in the past (before the event date). Note: the DatePicker has `in: date...` constraint so it may not allow selecting before the event date in the UI. If the constraint allows same-day or the dates are manipulated, proceed.<br>6. Tap "Save". | The `FormValidation.validateEventDates` function checks if `nextDueDate < eventDate`. If the custom next due date is before the event date, the validation message "Next due date must be after the event date." appears in red caption text under the Custom Date picker (this message only shows after `hasAttemptedSave` is true). An error haptic fires. The event is NOT saved. Note: In practice, the DatePicker uses `in: date...` which constrains selection to the event date or later, making this validation a secondary safeguard. |
| NE-012 | Health Event Validation | Save is blocked when no horse is selected | Multiple horses exist in the app (2 or more) | 1. Open "Log Health Event" with multiple horses passed in.<br>2. The horse picker shows "Choose..." as the default (nil) selection.<br>3. Do NOT select any horse from the picker -- leave it on "Choose...".<br>4. Fill in all other fields with valid data (type, date, etc.).<br>5. Observe the "Save" button state. | The Save button has `.disabled(selectedHorse == nil || isSaving)`. Since `selectedHorse` is nil (no horse selected), the Save button is disabled and cannot be tapped. The user must select a horse before saving. No error message is shown inline -- the button simply remains grayed out and non-interactive. |

---

## 2.3 Feed Board Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-013 | Feed Board Edge Cases | Toggle fed is ignored for horse with no feed schedule | A horse exists that has NO `FeedSchedule` assigned (i.e., `horse.feedSchedule` is nil) | 1. Navigate to the Feed Board tab.<br>2. Find the horse that has no feed schedule -- it should display "No feed schedule set" in secondary text.<br>3. Tap the circle toggle button (checkmark.circle.fill / circle) on that horse's row. | The `FeedBoardViewModel.toggleFed(for:allHorses:)` method contains `guard let schedule = horse.feedSchedule else { return }`. Since `feedSchedule` is nil, the function returns immediately without toggling. The circle icon remains unchanged (unfilled). The `isFed(horse:)` method also returns `false` because `feedSchedule` is nil. No undo banner appears. No haptic feedback occurs. The row continues to show "No feed schedule set". |
| NE-014 | Feed Board Edge Cases | Mark All Fed button is hidden when all horses are already fed | At least 2 horses exist with feed schedules | 1. Navigate to the Feed Board tab.<br>2. Observe the progress header showing "AM Progress" or "PM Progress" with a "Mark All" button visible.<br>3. Manually tap the toggle circle for each horse to mark them all as fed, one by one.<br>4. After the last horse is marked fed, observe the progress header area. | The `feedProgressHeader` computes `hasUnfed = viewModel.hasUnfedHorses(horses)`. Once all horses are fed, `hasUnfed` becomes false. The conditional `if hasUnfed { ... Mark All ... }` evaluates to false, causing the "Mark All" button to disappear with a `.scale.combined(with: .opacity)` transition animation. The progress bar shows full progress (e.g., "3/3 fed") in `Color.pastureGreen`. The "All [AM/PM] Feeds Complete" celebration banner appears at the top. |
| NE-015 | Feed Board Edge Cases | Undo correctly reverts when multiple toggles happen in quick succession | At least 2 horses exist with feed schedules, both unfed | 1. Navigate to the Feed Board tab.<br>2. Quickly tap the toggle circle for Horse A to mark it as fed.<br>3. Within 1-2 seconds (before the 4-second undo timer expires), quickly tap the toggle circle for Horse B to mark it as fed.<br>4. Observe the undo banner at the bottom of the screen.<br>5. Tap the "Undo" button on the banner. | When Horse A is toggled, an undo banner appears showing "Horse A marked as fed" with an Undo button. A 4-second `undoTimer` DispatchWorkItem is scheduled. When Horse B is toggled shortly after, the code calls `undoTimer?.cancel()` first (canceling Horse A's pending timer), then toggles Horse B's fed status. The undo banner updates to show "Horse B marked as fed". A new 4-second timer is set. Tapping "Undo" calls `undoLastToggle()`, which reverts ONLY Horse B (the `lastToggledHorse`). Horse A remains marked as fed. The undo banner disappears. |
| NE-016 | Feed Board Edge Cases | Search returns no results gracefully | At least one horse exists | 1. Navigate to the Feed Board tab.<br>2. Pull down or tap the search bar (`.searchable` modifier with prompt "Search horses...").<br>3. Type a search query that matches no horse name or owner name (e.g., "ZZZZXXX").<br>4. Observe the feed list area. | The `FeedBoardViewModel.filteredHorses` method filters by `localizedCaseInsensitiveContains(searchText)` on both `horse.name` and `horse.ownerName`. No horses match the query. The `ForEach(filtered)` iterates over an empty array, resulting in an empty section with no rows displayed. The progress header still shows based on all horses (not filtered), but no horse rows appear in the list. The search bar remains active with the entered text. |
| NE-017 | Feed Board Edge Cases | Deleting the last (only) horse returns to empty state | Exactly 1 horse exists in the app | 1. Navigate to the Feed Board tab. Verify the single horse is displayed.<br>2. Swipe left on the horse row to reveal the "Delete" swipe action (red trash icon).<br>3. Tap "Delete". A confirmation dialog appears titled "Delete Horse" with the message "This will permanently delete [horse name] and all associated health records and feed data. This cannot be undone."<br>4. Tap "Delete [horse name]" in the confirmation dialog. | The `modelContext.delete(horse)` call removes the horse from SwiftData. Due to the `@Relationship(deleteRule: .cascade)` annotations on `Horse`, the associated `HealthEvent` objects and `FeedSchedule` are also cascade-deleted. A warning haptic (`HapticManager.notification(.warning)`) fires. With `horses.isEmpty` now true, the `FeedBoardView` switches from `feedList` to `emptyStateView`, showing the horse icon, "No Horses Yet" title, descriptive text, and an "Add Horse" button. |

---

## 2.4 Subscription/Paywall Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-018 | Subscription/Paywall | Free tier blocks adding a second horse | Exactly 1 horse exists, `barnModeUnlocked` is false (free tier) | 1. Navigate to the Feed Board tab. Verify one horse is displayed.<br>2. Tap the "+" button in the top-right toolbar.<br>3. Observe what happens. | The `FeedBoardView` toolbar button checks `if !horses.isEmpty && !barnModeUnlocked`. Since one horse exists and `barnModeUnlocked` is false, `showPaywall` is set to true instead of `showingAddHorse`. The PaywallView sheet is presented, showing the "Barn Mode" title, crown icon, feature list (Unlimited horses, Full health tracking, PDF owner reports, Blanketing recommendations, Maintenance reminders), and the "Subscribe -- $19.99/month" button. The Add Horse form does NOT open. |
| NE-019 | Subscription/Paywall | First horse can be added on free tier without paywall | No horses exist, `barnModeUnlocked` is false (free tier) | 1. Start with a fresh app state (no horses).<br>2. Navigate to the Feed Board tab. The empty state view is shown.<br>3. Tap either the "+" button in the toolbar OR the "Add Horse" button in the empty state.<br>4. Observe what happens. | For the "+" toolbar button: the condition `!horses.isEmpty && !barnModeUnlocked` evaluates to false (because `horses.isEmpty` is true), so `showingAddHorse` is set to true. The Add Horse form opens directly without showing the paywall. For the "Add Horse" empty state button: it directly sets `viewModel.showingAddHorse = true`, bypassing the paywall check entirely. The user can add their first horse on the free tier. |
| NE-020 | Subscription/Paywall | Simulated subscribe sets barnModeUnlocked via @AppStorage | PaywallView is presented (free tier, 1 horse exists) | 1. Trigger the paywall by tapping "+" when 1 horse exists and `barnModeUnlocked` is false.<br>2. The PaywallView sheet appears.<br>3. Tap the "Subscribe -- $19.99/month" button.<br>4. Observe the app behavior.<br>5. Dismiss and tap "+" again. | The subscribe button action sets `barnModeUnlocked = true` (via `@AppStorage("barnModeUnlocked")`) and calls `dismiss()`. The PaywallView sheet closes. The `@AppStorage` write persists the value to `UserDefaults` under the key "barnModeUnlocked". On subsequent taps of "+", the condition `!barnModeUnlocked` is now false, so the paywall is skipped and the Add Horse form opens directly. In Settings, the Subscription section now shows "Barn Mode Active" with a checkmark.seal.fill icon and text "Unlimited horses and full features." The "Upgrade to Barn Mode" button is hidden. |
| NE-021 | Subscription/Paywall | Restore Purchases button is a no-op | PaywallView is presented | 1. Trigger the paywall by tapping "+" when 1 horse exists on free tier.<br>2. The PaywallView sheet appears.<br>3. Tap the "Restore Purchases" button (shown in secondary/caption style below the subscribe button).<br>4. Observe the app behavior. | The "Restore Purchases" button has an empty closure: `Button("Restore Purchases") { // StoreKit restore logic would go here }`. Nothing happens when tapped. No UI changes, no loading spinner, no confirmation, no error. The paywall remains displayed. The `barnModeUnlocked` value remains unchanged. This is expected MVP behavior -- real StoreKit restore logic is not yet implemented. |
| NE-022 | Subscription/Paywall | Dismissing paywall without subscribing keeps free tier | PaywallView is presented, `barnModeUnlocked` is false | 1. Trigger the paywall by tapping "+" when 1 horse exists on free tier.<br>2. The PaywallView sheet appears.<br>3. Tap the "X" button (xmark.circle.fill) in the top-right toolbar of the PaywallView.<br>4. The paywall sheet dismisses.<br>5. Verify the app state. | Tapping the dismiss button calls `dismiss()` without modifying `barnModeUnlocked`. The `@AppStorage("barnModeUnlocked")` value remains false. The user is returned to the Feed Board with their single horse. Tapping "+" again will present the paywall again. The Settings screen still shows "Free Plan" with "1 horse included. Upgrade for unlimited." and the "Upgrade to Barn Mode" button remains visible. |

---

## 2.5 Photo Handling

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-023 | Photo Handling | Corrupt or unloadable image data shows error message | App is open on Add Horse form | 1. Tap the "+" button to open the Add Horse sheet.<br>2. Tap the "Photo" row which activates the PhotosPicker.<br>3. Select an image from the photo library. Simulate a corrupt image scenario where `UIImage(data: data)` returns nil (e.g., a file that has a valid photos library entry but corrupted data), or where `loadTransferable` throws an error.<br>4. Observe the photo area of the form. | In the `loadPhoto(from:)` method: If the data loads but `UIImage(data: data)` returns nil, the `photoLoadingError` is set to "Unable to load image. Please try a different photo." and an error haptic fires. If the `loadTransferable` call throws an exception, `photoLoadingError` is set to "Photo loading failed: [error.localizedDescription]" and an error haptic fires. In both cases, `isLoadingPhoto` is set back to false. The error message appears in red caption text below the Photo row. The `imageData` remains nil, and the camera.circle.fill placeholder icon is displayed. The form remains editable and the user can attempt to select a different photo. |
| NE-024 | Photo Handling | Remove photo button clears image data | A photo has been successfully selected in the Add Horse form | 1. Open the Add Horse sheet and select a valid photo from the PhotosPicker.<br>2. Verify the photo thumbnail (44x44 circle-clipped image) appears in the Photo row.<br>3. Verify the "xmark.circle.fill" remove button appears next to the photo (in alertRed color).<br>4. Tap the remove button (labeled "Remove photo" for accessibility). | The remove button action sets `imageData = nil` and `selectedPhoto = nil`, and triggers `HapticManager.impact(.light)`. The photo thumbnail disappears and is replaced by the default camera.circle.fill icon in hunterGreen color. The xmark.circle.fill remove button disappears because the conditional `if imageData != nil` is now false. The form can be saved without a photo, or the user can select a new photo. |
| NE-025 | Photo Handling | Loading spinner displays during async photo load | App is open on Add Horse form | 1. Open the Add Horse sheet.<br>2. Tap the "Photo" row to open the PhotosPicker.<br>3. Select a large image file from the photo library.<br>4. Observe the Photo row immediately after selection. | When `selectedPhoto` changes, the `onChange` modifier triggers `loadPhoto(from:)`. This method sets `isLoadingPhoto = true` before beginning the async `item.loadTransferable(type: Data.self)` call. While `isLoadingPhoto` is true, the view displays a `ProgressView()` spinner inside a 44x44 frame instead of the photo thumbnail or camera icon. Once loading completes (success or failure), `isLoadingPhoto` is set to false on the MainActor, and the spinner is replaced by either the loaded photo thumbnail or an error message. |

---

## 2.6 Weather Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-026 | Weather Edge Cases | Location not determined shows permission request view | Location services not yet authorized (status is `.notDetermined`) or location denied | 1. On a fresh install or after resetting location permissions, navigate to the Weather tab.<br>2. Observe the weather card area. | When `locationManager.currentLocation` is nil and `weatherService.currentTemperatureF` is nil and `weatherService.errorMessage` is nil and `weatherService.isLoading` is false, the `requestPermissionView` is displayed. It shows a location.circle.fill icon (size 40, hunterGreen), the text "Enable location to get weather-based blanketing recommendations.", and an "Enable Location" button. Tapping the button calls `locationManager.requestLocation()`, which triggers `manager.requestWhenInUseAuthorization()` and `manager.requestLocation()`. If the user denies location access, the `locationManagerDidChangeAuthorization` callback updates the `authorizationStatus` but no location is provided, so the permission view remains. |
| NE-027 | Weather Edge Cases | WeatherKit API failure shows error view with retry | Location is authorized and available, but WeatherKit API call fails (e.g., no network) | 1. Enable location services for the app.<br>2. Disable the device's internet connection (airplane mode or Wi-Fi off).<br>3. Navigate to the Weather tab.<br>4. Observe the weather card after the fetch attempt. | The `WeatherService.fetchWeather(for:)` method catches the error and sets `errorMessage = "Unable to fetch weather: [error.localizedDescription]"`. The `WeatherDashboardView` detects that `weatherService.errorMessage` is non-nil and displays the `errorView` containing: an exclamationmark.icloud.fill icon in alertRed, the error message text in secondary style, and a "Retry" button. Tapping "Retry" calls `locationManager.requestLocation()` which triggers a new location update, which in turn triggers a new weather fetch via the `onChange(of: locationManager.currentLocation)` modifier. The blanketing section shows "Waiting for weather data..." since `currentTemperatureF` is nil. |
| NE-028 | Weather Edge Cases | No horses shows blanketing placeholder message | Weather data is loaded, but no horses exist in the app | 1. Ensure no horses exist in the app (delete all if needed).<br>2. Navigate to the Weather tab with location authorized and weather fetched.<br>3. Observe the "Blanketing Recommendations" section. | The weather card at the top displays temperature and conditions normally. In the blanketing section, the code checks `if horses.isEmpty` and displays the text "Add horses to see personalized blanketing advice." in body font with secondary foreground style, wrapped in an `.equineCard()` modifier. No individual horse blanket recommendation cards are shown. |
| NE-029 | Weather Edge Cases | Extreme low temperatures display correct recommendations | At least one horse exists (one clipped, one unclipped preferred), weather data is available | 1. Ensure at least one clipped horse and one unclipped horse exist.<br>2. Navigate to the Weather tab when the actual temperature is very low (below 30 degrees F), or test with a location known to have extreme cold.<br>3. Observe the blanketing recommendation cards for each horse. | The `BlanketRecommendation.recommend(temperatureF:isClipped:)` function computes a blanket recommendation based on the temperature and the horse's clipped status. At extreme low temperatures (e.g., below 30 degrees F), clipped horses will receive a heavier blanket recommendation than unclipped horses. Each horse's card shows: the horse name, a "Clipped" or "Unclipped" status badge, the recommendation name (e.g., "Heavy Blanket"), and a description. The weather notification system also checks `if temperature < 30` and adds a "extreme cold" score bonus of 10 points. |

---

## 2.7 Data Persistence Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-030 | Data Persistence | Cascade delete removes all associated HealthEvents and FeedSchedule | A horse exists with at least 2 health events and a feed schedule | 1. Navigate to the Feed Board tab.<br>2. Verify the horse shows feed schedule information and has logged health events (check the horse profile).<br>3. Swipe left on the horse row and tap the red "Delete" button.<br>4. In the confirmation dialog, tap "Delete [horse name]".<br>5. Navigate to any view that would show health events for that horse. | The `Horse` model defines `@Relationship(deleteRule: .cascade, inverse: \HealthEvent.horse) var healthEvents: [HealthEvent]` and `@Relationship(deleteRule: .cascade, inverse: \FeedSchedule.horse) var feedSchedule: FeedSchedule?`. When `modelContext.delete(horse)` is called, SwiftData cascade-deletes all related `HealthEvent` objects and the `FeedSchedule` object. The horse no longer appears on the Feed Board. The health events are no longer listed in the Health timeline. The feed schedule data is completely removed. No orphaned records remain in the SwiftData store. |
| NE-031 | Data Persistence | Data persists across force quit via SwiftData | A horse exists with a feed schedule and health events, some horses marked as fed | 1. Open the app and verify existing data (horse names, feed schedules, health events, fed status).<br>2. Mark at least one horse as fed on the Feed Board.<br>3. Force-quit the app by swiping up from the app switcher.<br>4. Relaunch the app.<br>5. Navigate to the Feed Board and verify the previously fed horse still shows as fed.<br>6. Open the horse's profile and verify health events are present. | SwiftData automatically persists all model objects (`Horse`, `HealthEvent`, `FeedSchedule`) to the underlying SQLite store. The `modelContext.insert(horse)` and any property mutations on `@Model` objects are saved by SwiftData's auto-save mechanism. After force quit and relaunch, all horses, their names, owner names, feed schedules (including `amFedToday`, `pmFedToday`, `amFedAt`, `pmFedAt` timestamps), and health events are restored. The `@AppStorage("barnModeUnlocked")` subscription state also persists via UserDefaults. |
| NE-032 | Data Persistence | CSV parsing correctly trims whitespace and filters empty strings | App is open on Add Horse form | 1. Tap "+" to open the Add Horse sheet.<br>2. Enter valid horse name and owner name.<br>3. In the "Supplements (comma-separated)" field under AM Feed, enter: "  SmartPak ,, Biotin , , CocoSoya  , ".<br>4. Save the horse.<br>5. Navigate to the horse's profile or Feed Board row and observe the supplements display. | The `StringUtilities.parseCSV` function processes the input: `text.split(separator: ",")` splits into [" SmartPak ", "", " Biotin ", " ", " CocoSoya ", " "]. Then `.map { $0.trimmingCharacters(in: .whitespaces) }` trims to ["SmartPak", "", "Biotin", "", "CocoSoya", ""]. Then `.filter { !$0.isEmpty }` removes empty strings, resulting in ["SmartPak", "Biotin", "CocoSoya"]. The Feed Board row displays these three supplements joined by commas: "SmartPak, Biotin, CocoSoya" with a leaf.fill icon. No empty entries or whitespace-only entries are shown. |

---

## 2.8 Notification Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-033 | Notification Edge Cases | Daily cap of 4 notifications prevents further sends | Notifications are enabled, multiple overdue events and weather alerts are triggerable | 1. Ensure `NotificationPreferences.shared.notificationsEnabled` is true.<br>2. Trigger conditions that would cause multiple notifications in rapid succession (e.g., multiple overdue horses, a weather threshold crossing, unfed horses, and an upcoming appointment).<br>3. After 4 notifications have been dispatched (confirmed by `dailyNotificationCount` reaching 4), trigger another notification-worthy event.<br>4. Observe whether the 5th notification is sent. | The `NotificationPreferences.canSendNotification(for:)` method calls `isDailyCapAvailable()`, which checks `dailyNotificationCount < Self.maxNotificationsPerDay` (where `maxNotificationsPerDay = 4`). After 4 notifications have been recorded via `recordNotificationSent(for:)`, the count reaches 4, and `isDailyCapAvailable()` returns false. All subsequent calls to `canSendNotification(for:)` return false for the remainder of the calendar day, regardless of moment type. The daily counter resets when `resetDailyCountIfNeeded()` detects that `dailyCountResetDate` is no longer today (i.e., the next calendar day). The Settings screen displays "4 per day" in the notification section. |
| NE-034 | Notification Edge Cases | Per-moment cooldowns prevent duplicate notifications | Notifications are enabled, an overdue cascade notification was just sent | 1. Trigger an overdue cascade notification (ensure overdue health events exist).<br>2. Verify the notification is delivered.<br>3. Immediately trigger another evaluation of the same moment type (e.g., by returning the app to the foreground, which calls `evaluateAllMoments`).<br>4. Observe whether a duplicate overdue notification is sent. | The `isCooldownElapsed(for:)` method checks `Date.now.timeIntervalSince(lastFired) >= type.cooldownInterval`. For `overdueCascade`, the cooldown is 24 hours (86400 seconds). For `coldSnapBlanket`, it is 12 hours. For `feedingStreak`, it is 24 hours. For `unfedAlert`, it is 4 hours. For `upcomingMaintenance`, it is 24 hours. Since the notification was just sent, the elapsed time is near zero, which is less than the cooldown interval. `canSendNotification(for:)` returns false, and no duplicate notification is dispatched. The cooldown must fully elapse before the same moment type can fire again. |
| NE-035 | Notification Edge Cases | Master toggle off prevents all notifications | Notifications master toggle is set to off in Settings | 1. Navigate to Settings > Notifications.<br>2. Toggle "Enable Notifications" OFF. This sets `notificationPrefs.notificationsEnabled = false`.<br>3. Create conditions that would normally trigger notifications: add overdue health events, have unfed horses past the deadline, and trigger a weather threshold crossing.<br>4. Return the app to the foreground (which calls `evaluateAllMoments`). | The `evaluateAllMoments` method checks `guard preferences.notificationsEnabled else { return }` as its first line. Since the master toggle is off, the method returns immediately without evaluating any moment types. No overdue, weather, feeding, or upcoming notifications are scheduled or delivered. Additionally, each individual moment evaluation (e.g., `evaluateOverdueCascade`) calls `preferences.canSendNotification(for:)`, which also checks `guard notificationsEnabled else { return false }` as its first check. The per-moment toggles in Settings are hidden when the master toggle is off (the `if notificationPrefs.notificationsEnabled { ... }` block in `notificationSection`). |
| NE-036 | Notification Edge Cases | First weather reading is stored but does not trigger notification | Notifications are enabled, `lastNotifiedTemperatureF` is nil (no previous reading) | 1. Ensure `NotificationPreferences.shared.lastNotifiedTemperatureF` is nil (fresh install or reset).<br>2. Allow the app to fetch weather data for the first time (ensure location is authorized).<br>3. The `evaluateWeatherMoment(temperature:horses:)` method is called with the current temperature.<br>4. Observe whether a weather/blanket notification is sent. | In `evaluateWeatherMoment`, the code checks `let lastTemp = preferences.lastNotifiedTemperatureF`. When `lastTemp` is nil (first reading), the code enters the `else` branch: `preferences.lastNotifiedTemperatureF = temperature; return`. This stores the current temperature for future comparison but does NOT trigger a notification. The method returns early before any threshold crossing or significant drop logic executes. This prevents a false positive notification on the very first weather reading where there is no previous temperature to compare against. Subsequent readings will compare against this stored value. |

---

## 2.9 PDF Report Edge Cases

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|-----------------|---------------|-----------------|-----------------|
| NE-037 | PDF Report Edge Cases | PDF report for horse with zero health events | A horse exists with NO health events logged (empty `healthEvents` array) | 1. Navigate to the horse's profile.<br>2. Trigger PDF report generation (e.g., via the owner report feature that calls `PDFReportService.generateReport(for: horse)`).<br>3. Open or preview the generated PDF. | The `PDFReportService.generateReport` method creates a valid PDF. The header section renders with the horse name, owner name, and generation date. In the "Health Events -- Last 30 Days" section, `horse.recentEvents` returns an empty array, so `recentEvents.isEmpty` is true and the body text "No health events recorded in the last 30 days." is drawn. In the "Upcoming Maintenance" section, `horse.upcomingEvents` returns an empty array, so the body text "No upcoming events scheduled." is drawn. The Analytics section shows "Total Recorded Costs: $0.00" and "Last 90 Days Spend: $0.00". No cost breakdown or farrier cycle insight is shown. The footer "Generated by EquineLog \| Confidential" is drawn at the bottom. The generated PDF passes the `data.count >= minimumValidPDFSize` (1000 bytes) check and is returned as valid `Data`. |
| NE-038 | PDF Report Edge Cases | PDF report with many events correctly inserts page breaks | A horse exists with more than 15-20 health events logged in the last 30 days (enough to fill a page) | 1. For a single horse, log many health events (15+ events with notes and costs) so that the event rows fill more than one US Letter page (792 points height).<br>2. Trigger PDF report generation via `PDFReportService.generateReport(for: horse)`.<br>3. Open or preview the generated PDF and check the page count. | In the `generateReport` method, as each event row is drawn, the code checks `if yPosition > pageRect.height - 100` (i.e., if `yPosition > 692`). When this condition is true, `context.beginPage()` is called to start a new PDF page and `yPosition` is reset to `margin` (50 points). This ensures event rows do not overflow past the bottom of the page. Similarly, before drawing the "Upcoming Maintenance" section, the code checks `if yPosition > pageRect.height - 150` (i.e., `yPosition > 642`), and before the Analytics section, it checks `if yPosition > pageRect.height - 200` (i.e., `yPosition > 592`). The resulting PDF has multiple pages with clean breaks. Each page has content starting from the top margin. The footer is drawn on the last page. No text is clipped or overlapping at page boundaries. |


---

# Section 3: iOS & Apple Review Specific — Network State Changes and Permission Handling

## 3.1 Permissions Handling

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-001 | Location Permission — Initial Deny | Verify that denying the CoreLocation permission on the initial `requestWhenInUseAuthorization()` prompt results in a graceful enable-location view on the Weather tab while all other tabs remain fully functional. | 1. Fresh install of EquineLog (no prior location permission decision recorded). 2. Device has network connectivity. 3. At least one horse profile exists in SwiftData so other tabs have data to display. | 1. Launch EquineLog for the first time and complete the onboarding flow. 2. Navigate to the Weather tab. 3. When the system dialog "Allow 'EquineLog' to use your location?" appears with options "Allow While Using App," "Allow Once," and "Don't Allow," tap **"Don't Allow."** 4. Observe the Weather tab UI after the dialog dismisses. 5. Navigate to the Horses tab and verify horse profiles load from local SwiftData storage. 6. Navigate to the Health/Log tab and verify entries display correctly. 7. Navigate to the Settings tab and verify all options are accessible. 8. Return to the Weather tab and confirm the enable-location view persists. | 1. The Weather tab displays an enable-location view (not a crash or blank screen) that clearly communicates location access is required for weather data. 2. The enable-location view includes a prompt or button directing the user to grant location permission (e.g., "Enable Location in Settings" or similar call-to-action). 3. The `LocationManager` singleton receives the `.denied` value via its `locationManagerDidChangeAuthorization` delegate callback and does not attempt further location requests. 4. The `WeatherService` does not attempt a WeatherKit API call without valid coordinates; `isLoading` remains `false` and `errorMessage` is set to a user-friendly location-related message. 5. All other tabs (Horses, Health/Log, Settings) function normally with full read/write access to local SwiftData persistence. 6. No console errors, crashes, or unhandled exceptions occur. 7. VoiceOver reads the enable-location view content accessibly, including the call-to-action button. |
| AR-002 | Location Permission — Grant Then Revoke | Verify that granting location permission, using weather features, and then revoking location permission via iOS Settings results in graceful error handling on the Weather tab with no crash, and that the `locationManagerDidChangeAuthorization` delegate properly handles the authorization status change. | 1. Fresh install of EquineLog with location permission not yet decided. 2. Device has network connectivity. 3. At least one horse profile exists. | 1. Launch EquineLog and complete onboarding. 2. Navigate to the Weather tab. 3. When the system location prompt appears, tap **"Allow While Using App."** 4. Verify weather data loads successfully via WeatherKit — confirm current conditions, temperature, and a valid "Last updated" timestamp are displayed. 5. Press the Home button or swipe up to move EquineLog to the background (do not terminate). 6. Open the iOS **Settings** app. 7. Navigate to **Settings > Privacy & Security > Location Services > EquineLog**. 8. Change the permission from "While Using the App" to **"Never."** 9. Return to EquineLog by tapping its icon or switching via the app switcher. 10. Observe the Weather tab UI immediately upon returning to the foreground. 11. Attempt to pull-to-refresh or tap any refresh/retry control on the Weather tab. 12. Navigate to the Horses tab and create or edit a horse profile to confirm local features remain unaffected. 13. Return to the Weather tab and confirm the error state persists. | 1. After revoking permission (step 9), the `LocationManager` singleton's `locationManagerDidChangeAuthorization` delegate fires with an updated `authorizationStatus` of `.denied`. 2. The Weather tab transitions from the previously loaded weather data to an error state or enable-location view — it does not continue showing stale data indefinitely without indication. 3. The `WeatherService` sets `isLoading` to `false` and populates `errorMessage` with a user-facing message indicating location access is needed. 4. No crash, hang, or unhandled exception occurs during the authorization status change or upon returning to the foreground. 5. Pull-to-refresh or retry controls do not trigger a crash; they re-check authorization status and maintain the error/enable-location view since permission is still denied. 6. The previously cached weather data (within the 15-minute cache window) may briefly display but the UI updates to reflect the denied status promptly. 7. All non-weather features (horse profiles, health logs, settings) continue to work with full SwiftData read/write capability. 8. VoiceOver announces the error state or enable-location view accessibly when the Weather tab content changes. |
| AR-003 | Notifications Permission — Initial Deny | Verify that denying the `UNUserNotificationCenter.requestAuthorization(options: [.alert, .sound, .badge])` prompt after onboarding allows the app to function fully without notifications, and that the Settings tab displays a warning about denied notification permission. | 1. Fresh install of EquineLog with no prior notification permission decision. 2. Device is not in Do Not Disturb or Focus mode (to avoid confounding factors). | 1. Launch EquineLog for the first time and proceed through the onboarding flow. 2. After onboarding completes, the app requests notification authorization. When the system dialog "'EquineLog' Would Like to Send You Notifications" appears with options "Allow" and "Don't Allow," tap **"Don't Allow."** 3. Observe that the app continues to its main interface without interruption. 4. Navigate to the Horses tab and add a new horse profile with name, breed, and photo — confirm the save completes successfully. 5. Navigate to the Health/Log tab and create a new log entry — confirm it persists in SwiftData. 6. Navigate to the Weather tab (with location granted) and confirm weather data loads via WeatherKit. 7. Navigate to the **Settings** tab. 8. Locate the notification permission status indicator or warning within the Settings view. 9. Verify the `BGTaskScheduler` background task with identifier `com.equinelog.notificationEvaluation` is registered but does not cause errors when notification delivery is not authorized. | 1. The app does not crash, freeze, or show an error screen after the user taps "Don't Allow" on the notification authorization prompt. 2. All core features (horse management, health logging, weather) operate identically to the notifications-granted state. 3. The `NotificationService` checks `authorizationStatus` and finds it `.denied`. 4. The Settings tab displays a visible warning element (e.g., a yellow/orange banner, an inline warning label, or an alert icon) indicating that notifications are disabled, with guidance such as "Notifications are disabled. Enable them in Settings to receive reminders." 5. The warning in Settings includes a tappable link or button that opens the EquineLog entry in iOS Settings (via `UIApplication.openSettings` or deep link to **Settings > EquineLog > Notifications**) so the user can re-enable notifications. 6. The `BGTaskScheduler` task with ID `com.equinelog.notificationEvaluation` is registered without error but gracefully skips notification delivery when `authorizationStatus` is `.denied`. 7. No phantom badge counts appear on the app icon. 8. VoiceOver reads the notification warning in Settings as an accessible element with appropriate traits. |
| AR-004 | Notifications Permission — Grant Then Revoke | Verify that granting notification permission initially and then revoking it via iOS Settings causes the Settings tab to update its notification permission status display and show the denied-state warning on next app foreground or Settings tab visit. | 1. Fresh install of EquineLog. 2. Notification permission has been granted (user tapped "Allow" during post-onboarding prompt). 3. At least one notification-worthy event is configured (e.g., a health reminder) to confirm notifications were previously working. | 1. Launch EquineLog and confirm the app is in normal operation with notifications granted. 2. Navigate to the Settings tab and verify that no notification warning is displayed (permission is currently `.authorized`). 3. Press the Home button or swipe up to move EquineLog to the background. 4. Open the iOS **Settings** app. 5. Navigate to **Settings > EquineLog > Notifications** (or **Settings > Notifications > EquineLog**). 6. Toggle **"Allow Notifications"** to **OFF**. 7. Return to EquineLog via the app switcher or by tapping its icon. 8. Navigate to the **Settings** tab within EquineLog. 9. Observe the notification permission status indicator. 10. Navigate away from Settings to another tab and then return to Settings to confirm the warning persists. 11. Confirm that all other app features (horse profiles, health logs, weather) continue to function without any degradation. | 1. Upon returning to the foreground (step 7), the `NotificationService` re-checks `authorizationStatus` and detects the change from `.authorized` to `.denied`. 2. The Settings tab displays the notification denied warning (same warning element described in AR-003: visible banner or inline warning with guidance text). 3. The warning includes an actionable element (button or link) to navigate the user to **Settings > EquineLog > Notifications** to re-enable notifications. 4. The app does not crash or freeze when the notification permission status changes while the app is in the background. 5. The `BGTaskScheduler` task with ID `com.equinelog.notificationEvaluation` continues to be registered but the `NotificationService` skips scheduling or delivering local notifications when `authorizationStatus` is `.denied`. 6. Previously scheduled local notifications that are now undeliverable do not cause errors or orphaned pending notification requests. 7. All non-notification features remain fully functional with no performance degradation. 8. The warning state accurately reflects the current permission status on every visit to the Settings tab (no stale cached status). |
| AR-005 | Photo Library Permission — Deny Access | Verify that when the user denies photo library access (or selects no photos), the PhotosPicker component handles the denial internally and the horse profile saves successfully without a photo. | 1. EquineLog is installed and onboarding is complete. 2. At least one horse profile exists or the user is prepared to create one. 3. The device has photos in the Photo Library. | 1. Navigate to the **Horses** tab. 2. Tap the button to add a new horse or edit an existing horse profile. 3. In the horse profile editor, tap the photo area or "Add Photo" button that presents the `PhotosPicker` from the PhotosUI framework. 4. When the system **PhotosPicker** modal appears, observe that it presents the system photo selection UI (this is an out-of-process picker that manages its own permissions). 5. Instead of selecting a photo, tap **"Cancel"** or dismiss the picker without selecting an image. 6. Fill in the remaining horse profile fields (name, breed, date of birth, etc.). 7. Tap **"Save"** to persist the horse profile. 8. Verify the horse profile appears in the horse list with a placeholder image or default avatar instead of a photo. 9. Tap on the saved horse to open its detail view and confirm all data fields are correctly persisted in SwiftData. 10. Repeat steps 2–4, but this time when the PhotosPicker appears, if the system prompts for photo library access with "Select Photos…" or "Allow Full Access," choose **"Don't Allow"** (if presented) or simply dismiss without selecting. 11. Save the horse profile again and verify it saves without error. | 1. The `PhotosPicker` (PhotosUI) handles all permission interactions internally — EquineLog does not present its own permission alert for photo access, and no crash occurs regardless of the user's photo library permission state. 2. When no photo is selected (either by canceling or by denying access), the horse profile form remains in a valid state with a placeholder image displayed in the photo area. 3. The horse profile saves successfully to SwiftData without a photo — the photo field is `nil` or empty, and this does not trigger a validation error or crash. 4. The horse profile list displays the saved horse with a default placeholder image (e.g., a horse silhouette icon or generic avatar) that is visually consistent with the app's design. 5. The horse detail view renders all persisted data fields correctly, with the placeholder image displayed where the photo would normally appear. 6. No error messages, alerts, or toasts appear related to photo access denial. 7. The user can later edit the horse profile and successfully add a photo if they choose to grant access in a subsequent PhotosPicker session. 8. VoiceOver labels the placeholder image appropriately (e.g., "No photo, horse profile image" or similar accessible description) rather than leaving it unlabeled or reading it as a broken image. |

## 3.2 Network and Connectivity

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-006 | Airplane Mode — Full App Usage | Verify that enabling Airplane Mode (fully severing network connectivity) allows all local-first SwiftData features to operate normally while the Weather tab displays an appropriate error with a retry mechanism. | 1. EquineLog is installed with all permissions granted (location, notifications). 2. At least one horse profile and one health log entry exist in SwiftData. 3. Device currently has network connectivity and weather data has been previously loaded (cache may or may not be within the 15-minute window). | 1. Open **Control Center** (swipe down from the top-right on Face ID devices, or swipe up from the bottom on Touch ID devices) and enable **Airplane Mode** — confirm the airplane icon appears in the status bar and Wi-Fi/Cellular indicators disappear. 2. Launch EquineLog (or return to it if already open). 3. Navigate to the **Horses** tab. Add a new horse profile with name, breed, and notes. Tap Save and confirm the profile persists in SwiftData and appears in the list. 4. Edit an existing horse profile — change the name and save. Verify the edit persists. 5. Navigate to the **Health/Log** tab. Create a new health log entry with date, type, and description. Save and verify it appears in the log list. 6. Navigate to the **Settings** tab. Verify all settings are accessible and any locally-stored preferences can be modified. 7. Navigate to the **Weather** tab. 8. Observe the Weather tab UI state — note whether cached data is displayed (if within 15-minute cache window) or an error state is shown. 9. Wait until any cached data expires (if applicable) or attempt a pull-to-refresh / force refresh action. 10. Observe the error state and any retry mechanism displayed on the Weather tab. 11. Return to the Horses tab and delete a horse profile to confirm destructive local operations also work offline. | 1. All local-first features powered by SwiftData operate without any degradation in Airplane Mode: creating, reading, updating, and deleting horse profiles and health log entries all succeed. 2. The Weather tab behavior depends on cache state: (a) If cached weather data exists within the 15-minute `WeatherService` cache window, the cached data is displayed with the original "Last updated" timestamp. (b) Once the cache expires or if no cache exists, the Weather tab displays an error state. 3. The `WeatherService` sets `isLoading` to `true` during the fetch attempt, then sets it back to `false` when the network request fails. The `errorMessage` property is populated with a user-friendly message such as "Unable to load weather data. Check your connection and try again." 4. A retry button or pull-to-refresh mechanism is visible and functional on the Weather tab error state — tapping it re-attempts the WeatherKit fetch (which will fail again in Airplane Mode but does not crash or hang). 5. No indefinite loading spinners appear — `isLoading` transitions to `false` within a reasonable timeout period (no longer than 30 seconds). 6. The Settings tab does not show spurious errors related to network unavailability. 7. The `BGTaskScheduler` task (`com.equinelog.notificationEvaluation`) does not crash if it fires while offline; it completes its handler gracefully. 8. No crash logs, unhandled exceptions, or ANR (Application Not Responding) events are generated during offline use. |
| AR-007 | Network Loss Mid-Weather Fetch | Verify that losing network connectivity while a WeatherKit fetch is in-flight causes the `WeatherService` to properly transition `isLoading` back to `false`, display an error message, and not leave the UI in a hung or indeterminate loading state. | 1. EquineLog is installed with location permission granted. 2. Device has active network connectivity (Wi-Fi or Cellular). 3. The Weather tab's 15-minute cache has expired or the user is prepared to force-refresh to trigger a new WeatherKit API call. 4. Tester is ready to toggle Airplane Mode quickly via Control Center. | 1. Launch EquineLog and navigate to the **Weather** tab. 2. If weather data is currently displayed and cached, wait for the 15-minute cache to expire or use the force-refresh mechanism to trigger a new fetch. 3. At the moment the weather fetch begins (indicated by a loading spinner or `isLoading` state becoming `true`), **immediately** open Control Center and enable **Airplane Mode** to sever network connectivity mid-request. 4. Observe the Weather tab UI — monitor whether the loading spinner continues indefinitely or resolves. 5. Wait up to 30 seconds and note the final state of the Weather tab. 6. Verify the `isLoading` state has returned to `false` (loading indicator is no longer visible). 7. Verify that an error message is displayed on the Weather tab. 8. Disable Airplane Mode via Control Center to restore connectivity. 9. Do **not** yet interact with the Weather tab — observe whether it automatically retries or waits for user action. 10. Navigate to the Horses tab and back to the Weather tab to confirm the error state persists until a manual retry is performed. | 1. The `WeatherService` `isLoading` property transitions from `true` back to `false` within a reasonable timeout (no longer than 30 seconds) after network connectivity is lost. The loading spinner or loading indicator disappears. 2. The `WeatherService` `errorMessage` property is populated with a user-friendly error message (e.g., "Unable to load weather data. Check your connection and try again." or similar) — not a raw technical error string or stack trace. 3. The Weather tab does not hang in a perpetual loading state. The UI is fully interactive (scrollable, tappable) after the error state is reached. 4. No crash, force-close, or ANR event occurs due to the interrupted network request. 5. The previously cached weather data (if any existed before the failed fetch) is either still displayed alongside the error notification, or is replaced by the error view — but the UI is never blank without explanation. 6. After restoring network (step 8), the Weather tab does not automatically re-fetch without user interaction — the error state and retry control remain visible, respecting the user's control over data usage. 7. All non-weather tabs remain fully functional throughout the test, with no side effects from the failed weather request. 8. VoiceOver announces the error state change when the loading indicator is replaced by the error message, ensuring accessibility for users who cannot visually observe the transition. |
| AR-008 | Network Restore After Weather Failure | Verify that after a weather fetch failure due to network unavailability, restoring network connectivity and triggering a retry successfully fetches weather data from WeatherKit, displays it on the Weather tab, and updates the cache timestamp. | 1. EquineLog is installed with location permission granted. 2. Device is currently in **Airplane Mode** (or otherwise has no network connectivity). 3. The Weather tab is displaying an error state from a prior failed fetch attempt (e.g., as a result of completing AR-006 or AR-007). 4. The `WeatherService` 15-minute cache is expired or contains no valid cached data. | 1. Confirm the Weather tab is currently in an error state with the `WeatherService` `errorMessage` displayed and a retry mechanism visible (retry button or pull-to-refresh). 2. Confirm `isLoading` is `false` (no loading spinner is visible). 3. Open **Control Center** and disable **Airplane Mode** — wait a few seconds for the device to reconnect to Wi-Fi or Cellular (confirm connectivity by checking for the Wi-Fi or cellular signal icon in the status bar). 4. Return to EquineLog and navigate to the Weather tab (if not already on it). 5. Tap the **retry button** or perform a **pull-to-refresh** gesture on the Weather tab to initiate a new WeatherKit fetch. 6. Observe the loading state — confirm a loading indicator appears (`isLoading` transitions to `true`). 7. Wait for the weather data to load. 8. Once data appears, verify the following UI elements are populated: current temperature, weather condition description/icon, and location name. 9. Locate and verify the "Last updated" timestamp — confirm it reflects the current time (within the last 1–2 minutes). 10. Navigate away from the Weather tab to the Horses tab. 11. Navigate back to the Weather tab within 15 minutes. 12. Verify the cached data is displayed immediately without a new fetch (the "Last updated" timestamp should match the time from step 9, not the current time). 13. Force refresh the weather data and confirm the timestamp updates to the new current time, demonstrating that the 15-minute cache can be bypassed by explicit user action. | 1. Upon tapping retry or performing pull-to-refresh (step 5), the `WeatherService` `isLoading` transitions to `true` and a visible loading indicator (spinner or progress indicator) appears on the Weather tab. 2. The `WeatherService` `errorMessage` is cleared (set to `nil` or empty) once the fetch begins or succeeds. 3. The WeatherKit API call succeeds and weather data populates the Weather tab: current temperature is displayed in the user's preferred unit, a weather condition icon and description are shown, and the location name matches the device's current location. 4. The "Last updated" timestamp on the Weather tab displays the current date and time, confirming the data is freshly fetched and not served from a stale cache. 5. The `WeatherService` 15-minute cache is populated with the new data — navigating away and returning to the Weather tab within 15 minutes (steps 10–12) serves the cached data instantly without triggering a new network request or showing a loading spinner. 6. The force-refresh mechanism (step 13) bypasses the 15-minute cache and triggers a new WeatherKit fetch, resulting in an updated "Last updated" timestamp. 7. The Weather tab transitions cleanly from error state to loaded state with no visual glitches, layout shifts, or flickering. 8. No duplicate or overlapping fetch requests are triggered by rapid retry taps (the retry mechanism is debounced or disabled while `isLoading` is `true`). 9. VoiceOver announces the updated weather data when it loads, and the "Last updated" timestamp is accessible as a readable text element. |


# EquineLog iOS Test Plan -- Section 4: iOS & Apple Review Specific Tests

## 3.3 State Restoration and Lifecycle

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-009 | State Restoration | Background/foreground cycle preserves state and triggers scenePhase evaluation | 1. App installed and onboarding completed. 2. At least one horse added with a feed schedule. 3. Notifications enabled. | 1. Open the app and navigate to the Feed Board (Stable tab). 2. Mark one horse as fed by tapping the feed toggle circle. 3. Verify the horse shows the "Fed" StatusBadge and the progress header updates (e.g., "1/2 fed"). 4. Press the Home button or swipe up to send the app to background. 5. Wait 5 seconds. 6. Return to the app by tapping the EquineLog icon. 7. Observe the Feed Board state. 8. Check the Console or debug output for `NotificationService.shared.evaluateAllMoments` invocation. | 1. Upon returning to foreground, the Feed Board displays the same tab (Stable) with the same horse still marked as fed (SwiftData persisted the `amFedToday`/`pmFedToday` and `amFedAt`/`pmFedAt` values). 2. The progress header still reads "1/2 fed" (or equivalent). 3. The `scenePhase == .active` handler in `ContentView.onChange(of: scenePhase)` fires, calling `NotificationService.shared.evaluateAllMoments(horses:weatherService:)` to re-evaluate high-value notification moments. 4. The selected tab (`@State selectedTab`) remains `.stable` because SwiftUI preserves `@State` across background/foreground without process termination. 5. No crashes, no visual glitches, no data loss. |
| AR-010 | State Restoration | Force quit during form entry loses @State but does not crash on relaunch | 1. App installed and onboarding completed. 2. At least one horse exists (so the paywall gate is relevant). | 1. Open the app and tap the "+" button (plus.circle.fill) in the Feed Board toolbar to open AddHorseView. 2. Type "Starlight" in the Name TextField and "Jane Doe" in the Owner Name TextField. 3. Select a photo using the PhotosPicker. 4. Fill in AM Grain with "2 qt SafeChoice" and AM Hay with "2 flakes Timothy". 5. Toggle "Clipped (Shorn)" to ON. 6. Do NOT tap Save. 7. Force quit the app: on iPhone, swipe up from bottom to open the App Switcher, then swipe the EquineLog card upward to terminate. 8. Relaunch the app by tapping the EquineLog icon. 9. Observe the initial screen and app state. | 1. The app launches without crashing -- no `fatalError` from the `ModelContainer` initialization in `EquineLogApp.init()`. 2. The app opens to the main Feed Board (not the AddHorseView form), because AddHorseView was presented as a `.sheet` and all form fields (`@State private var name`, `ownerName`, `imageData`, `amGrain`, `amHay`, `isClipped`, etc.) are transient `@State` properties that are destroyed when the process terminates. 3. No horse named "Starlight" exists in the Feed Board list, confirming that `modelContext.insert(horse)` was never called. 4. The previously existing horse(s) still appear in the Feed Board, confirming SwiftData persistence is intact. 5. The `hasCompletedOnboarding` UserDefaults flag remains `true`, so OnboardingView is not shown again. 6. If `shouldShowGuidedTour` is true (tour not completed), the guided tour restarts from `.welcomeBack` via the `.onAppear` handler in ContentView. |
| AR-011 | Memory Warning | Memory warning handled gracefully with externalStorage images and SwiftData pressure management | 1. App installed with at least 3 horses, each having a photo (`imageData` populated). 2. Device or simulator configured to simulate memory warnings. | 1. Open the app and navigate to the Feed Board. 2. Scroll through the horse list to load all `HorseAvatarView` instances (which read `horse.imageData`). 3. Navigate to a horse profile by tapping a NavigationLink row. 4. Simulate a memory warning (Xcode: Debug > Simulate Memory Warning; or on device, open multiple heavy apps to trigger system pressure). 5. Return to the Feed Board and scroll through the list again. 6. Navigate to the Weather Dashboard tab and verify blanketing recommendation cards render with horse names. 7. Return to the Feed Board and verify all horse avatars still load. | 1. No crash occurs during or after the memory warning. 2. Because `Horse.imageData` is declared with `@Attribute(.externalStorage)`, SwiftData stores photo data as separate files on disk rather than inline in the SQLite row. This means image data does not bloat the in-memory object graph and can be faulted in/out by the framework. 3. After the memory warning, SwiftData may release faulted objects from memory; when the Feed Board is revisited, `@Query(sort: \Horse.name)` re-fetches and horse rows re-render with avatars loading from external storage. 4. All horse names, feed schedules, and health events remain intact (SwiftData persisted to disk). 5. No visual corruption in `HorseAvatarView` -- images reload from the external storage path. 6. The Weather Dashboard still shows blanket recommendation cards for all horses with correct clip status. |
| AR-012 | State Restoration | Guided tour resumes from welcomeBack step after app restart when tour was not completed | 1. App installed. 2. Onboarding completed with experienceLevel set to `.newToApps` or `.someExperience` (not `.techSavvy`, which sets `hasCompletedGuidedTour = true`). 3. The guided tour has been started but NOT completed (e.g., user was on the `.feedBoardIntro` step). | 1. Open the app. The GuidedTourOverlay should be visible showing one of the tour steps (e.g., "Your Feed Board"). 2. Advance the tour to the `.tapToFeed` step by tapping "Next". 3. Force quit the app (swipe up in App Switcher to terminate). 4. Relaunch the app. 5. Observe the initial state of the app after launch. 6. Check whether the GuidedTourOverlay appears and which step it shows. 7. Tap through the entire tour to completion. | 1. After relaunch, `OnboardingManager.shouldShowGuidedTour` returns `true` because `hasCompletedOnboarding == true` (persisted in UserDefaults) and `hasCompletedGuidedTour == false` (persisted in UserDefaults as `false`). 2. The `guidedTourStep` property is `nil` after relaunch because it is a transient (non-persisted) `var guidedTourStep: GuidedTourStep?` on the `@Observable` class. 3. The `.onAppear` handler in `ContentView` detects `shouldShowGuidedTour == true && guidedTourStep == nil` and calls `onboardingManager.startGuidedTour()` after a 0.6-second delay. 4. `startGuidedTour()` sets `guidedTourStep = .welcomeBack`, so the tour always restarts from the beginning ("Welcome to Your Barn!") rather than resuming from the step where the user left off. 5. The GuidedTourOverlay appears with the `.welcomeBack` step showing title "Welcome to Your Barn!" and button label "Let's Go". 6. The step dots indicator reflects the full tour sequence for the user's `primaryUseCase`. 7. Completing the tour by tapping through all steps and reaching `.tourComplete` then tapping "Start Managing" calls `completeGuidedTour()`, which sets `hasCompletedGuidedTour = true` in UserDefaults and `guidedTourStep = nil`. 8. Subsequent relaunches do NOT show the guided tour. |

## 3.4 UI Adaptability

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-013 | iPad Layout | iPad displays NavigationSplitView with sidebar when horizontalSizeClass is .regular | 1. iPad device or iPad simulator (e.g., iPad Pro 12.9"). 2. App installed and onboarding completed. 3. At least one horse added. | 1. Launch the app on the iPad in landscape or full-screen portrait. 2. Observe the navigation structure. 3. Verify the left sidebar is visible with a list of tabs. 4. Tap each sidebar item: "Stable", "Health", "Weather", "Settings". 5. Verify the detail pane on the right updates to show the corresponding view. 6. Check the navigation title in the sidebar reads "EquineLog". 7. Verify the sidebar uses `.listStyle(.sidebar)`. 8. Check the tint color of selected sidebar items. | 1. The app displays a `NavigationSplitView` because `horizontalSizeClass == .regular` in `ContentView`. 2. The sidebar shows a `List` of `AppTab.allCases` with Labels: "Stable" (house.fill), "Health" (heart.text.clipboard), "Weather" (cloud.sun.fill), "Settings" (gearshape.fill). 3. The sidebar navigation title is "EquineLog". 4. Tapping "Stable" shows `FeedBoardView` in the detail pane with its own "Feed Board" navigation title. 5. Tapping "Health" shows `HealthTimelineView` with "Maintenance" navigation title. 6. Tapping "Weather" shows `WeatherDashboardView` with "Weather" navigation title. 7. Tapping "Settings" shows `SettingsView` with "Settings" navigation title. 8. The `.tint(.hunterGreen)` modifier is applied to the NavigationSplitView, so selected sidebar items and interactive elements use the hunterGreen color (RGB 0.13, 0.37, 0.18). 9. No TabView is rendered on iPad. |
| AR-014 | iPhone Layout | iPhone displays TabView with 4 tabs and hunterGreen tint | 1. iPhone device or iPhone simulator (e.g., iPhone 15 Pro). 2. App installed and onboarding completed. 3. At least one horse added. | 1. Launch the app on the iPhone. 2. Observe the bottom tab bar. 3. Verify exactly 4 tabs are displayed with correct labels and icons. 4. Tap each tab and verify the correct view loads. 5. Verify the tab bar tint color is hunterGreen. 6. Rotate to landscape and verify the tab bar remains. | 1. The app displays a `TabView` because `horizontalSizeClass != .regular` in `ContentView`. 2. Four tabs appear in the bottom tab bar: (a) "Stable" with SF Symbol "house.fill", (b) "Health" with SF Symbol "heart.text.clipboard", (c) "Weather" with SF Symbol "cloud.sun.fill", (d) "Settings" with SF Symbol "gearshape.fill". 3. The default selected tab is `.stable` (the `@State selectedTab` default). 4. Tapping "Stable" shows `FeedBoardView`, "Health" shows `HealthTimelineView`, "Weather" shows `WeatherDashboardView`, "Settings" shows `SettingsView`. 5. The `.tint(.hunterGreen)` modifier applies to the TabView, so the selected tab icon and label render in hunterGreen (RGB 0.13, 0.37, 0.18). 6. No sidebar or split view is rendered on iPhone. 7. In landscape, the tab bar adjusts but remains functional and all tabs are accessible. |
| AR-015 | Dynamic Type | Largest Dynamic Type setting renders all text without truncation or overlap | 1. Any iOS device or simulator. 2. App installed and onboarding completed with at least 2 horses. 3. Set Dynamic Type to largest non-accessibility size: Settings > Display & Brightness > Text Size > drag slider to maximum. | 1. Open the app and navigate to the Feed Board (Stable tab). 2. Verify all horse names, feed details (grain, hay, supplements, medications, special instructions), slot indicator ("AM Feed" / "PM Feed"), and progress header text ("X/Y fed") are fully visible without truncation. 3. Navigate to a horse profile and verify all section headers, feed schedule details, and health event summaries remain readable. 4. Navigate to the Health tab and verify filter chip labels ("All", "Farrier", "Vet", "Deworming", "Dental") and event row text are legible. 5. Navigate to the Weather tab and verify temperature text, humidity, wind speed, and blanket recommendation text are readable. 6. Navigate to Settings and verify all section text, subscription info, notification toggles, and version info are readable. 7. Open the PaywallView and verify the "$19.99/month" price, feature list, and legal disclaimer text scale appropriately. | 1. All text in the app scales because every text element uses `EquineFont` (which wraps `Font.system(.body)`, `.caption`, `.headline`, `.largeTitle`, `.title2`, `.subheadline`) -- all semantic system text styles that respond to Dynamic Type. 2. On the Feed Board: horse names using `EquineFont.headline`, feed details using `EquineFont.feedBoard` (monospaced subheadline), captions using `EquineFont.caption`, and the slot indicator text all scale proportionally. 3. On the Health Timeline: filter chip text using `EquineFont.caption` and event row text using `EquineFont.headline`/`.body`/`.caption` scale up. The horizontal ScrollView for filter chips allows overflow to scroll rather than truncate. 4. On the Weather Dashboard: temperature uses `EquineFont.largeTitle`, condition description uses `EquineFont.body`, humidity/wind labels use `EquineFont.caption` -- all scale. 5. On the PaywallView: "Subscribe -- $19.99/month" uses `EquineFont.headline`, feature rows use `EquineFont.body`, legal text uses `.caption2` (system style, scales). 6. The `specialInstructions` text in `FeedBoardRow` is limited to `lineLimit(2)` but uses `.fixedSize(horizontal: false, vertical: true)` so it can expand vertically. 7. No text is clipped by fixed-size frames. Scrollable containers (List, ScrollView) accommodate enlarged content. |
| AR-016 | Landscape | Landscape orientation on iPhone renders correctly with no layout breaks | 1. iPhone device or simulator. 2. App installed and onboarding completed with at least 2 horses. 3. Portrait orientation lock is disabled. | 1. Open the app in portrait orientation on the Stable tab. 2. Rotate the device to landscape. 3. Verify the Feed Board list renders correctly with horse rows, feed toggle circles, and the progress header. 4. Navigate to the Health tab in landscape and verify filter chips scroll horizontally and event rows display. 5. Navigate to the Weather tab in landscape and verify the weather card and blanketing recommendation cards render without overlap. 6. Open the PaywallView by tapping the "+" button when the free-tier gate triggers, and verify the paywall lays out correctly in landscape. 7. Open the AddHorseView form and verify all form sections scroll and fields are accessible. 8. Rotate back to portrait and verify layout restores. | 1. In landscape, the TabView's tab bar compresses to icon-only or icon+label in a compact form -- all 4 tabs (Stable, Health, Weather, Settings) remain tappable. 2. The Feed Board List uses `.listStyle(.insetGrouped)` which adapts to landscape width. Horse rows expand horizontally: the `HStack` in `FeedBoardRow` (avatar, feed content, quick log button, fed toggle) spreads to fill available width. 3. The Health Timeline's horizontal ScrollView for filter chips has more horizontal space in landscape, showing more chips without scrolling. 4. The Weather Dashboard's `ScrollView` with `VStack(spacing: 20)` and `.equineCard()` cards stretch to the wider width. 5. The PaywallView's `VStack(spacing: 32)` with Spacers adapts vertically; the "Subscribe" button `frame(maxWidth: .infinity)` stretches to fill width. 6. The AddHorseView Form sections scroll vertically in landscape with adequate space for all TextFields. 7. No content is clipped, overlapping, or inaccessible. 8. Returning to portrait restores the layout without artifacts. |
| AR-017 | Small Screen | iPhone SE (smallest screen) renders all critical UI without clipping or inaccessibility | 1. iPhone SE (3rd generation, 4.7" screen) device or simulator. 2. App installed and onboarding completed with 1 horse with a full feed schedule (grain, hay, supplements, medications, special instructions). | 1. Open the app on iPhone SE. 2. Verify the tab bar displays all 4 tabs with icons and labels. 3. On the Feed Board, verify the horse row fits: avatar (50pt), feed content, quick log button, and fed toggle circle. 4. Verify the slot indicator capsule ("AM Feed" / "PM Feed") and the "+" toolbar button both fit in the navigation bar. 5. Verify the progress header shows the progress bar and "Mark All" button if applicable. 6. Navigate to the Health tab. Verify filter chips are scrollable and event rows are not truncated. 7. Navigate to the Weather tab. Verify temperature and humidity/wind details are readable. 8. Open PaywallView and verify the crown icon, title, feature list, Subscribe button, Restore Purchases button, and legal disclaimer are all visible by scrolling. 9. Open AddHorseView and verify all form sections (Horse Details, Coat Status, Quick Fill, AM Feed, PM Feed, Special Instructions) are scrollable and accessible. | 1. The tab bar renders compactly on iPhone SE with all 4 tab labels visible (system behavior for TabView). 2. The `FeedBoardRow` HStack renders at the SE's ~375pt width: the `HorseAvatarView(size: 50)` takes 50pt, the quick log button circle takes ~40pt, the fed toggle takes ~28pt, and the remaining ~240pt accommodates `feedContent` VStack with horse name, feed details (EquineFont.feedBoard, monospaced subheadline), supplement/medication rows, and special instructions (line-limited to 2). 3. The navigation bar's leading `slotIndicator` capsule and trailing "+" button both fit because the capsule uses `EquineFont.caption` (compact text). 4. On the Health tab, the horizontally scrolling filter chips handle overflow correctly on the narrow screen. 5. On the Weather tab, the `currentWeatherDisplay` HStack stacks the weather icon (48pt) and text VStack side by side within ~375pt width. 6. The PaywallView scrolls vertically with Spacers collapsing on the shorter screen. The feature list rows, Subscribe button, and legal text remain accessible. 7. The AddHorseView Form is scrollable by default, so all sections are reachable. 8. No critical interactive elements (buttons, toggles, text fields) are hidden off-screen or rendered at less than 44pt tap target height. |
| AR-018 | Dark Mode | Dark mode toggle produces a known limitation: hardcoded colors do not adapt | 1. Any iOS device or simulator. 2. App installed and onboarding completed. | 1. Open the iOS Settings app. 2. Navigate to Display & Brightness. 3. Select "Dark" appearance. 4. Return to the EquineLog app. 5. Observe the Feed Board for text and background contrast. 6. Navigate to each tab (Health, Weather, Settings) and observe color rendering. 7. Open the PaywallView and observe the feature list, button, and legal text colors. 8. Open the GuidedTourOverlay (if tour not completed) and observe tooltip card colors. | 1. KNOWN LIMITATION: The app does NOT support Dark Mode. All custom colors are hardcoded RGB values in `EquineTheme.swift`: `hunterGreen` (0.13, 0.37, 0.18), `parchment` (0.97, 0.95, 0.91), `barnText` (0.15, 0.12, 0.10), `saddleBrown` (0.55, 0.27, 0.07), `alertRed` (0.78, 0.15, 0.12), `pastureGreen` (0.20, 0.62, 0.28), `fenceLine` (0.80, 0.75, 0.68). None use adaptive Color assets. 2. When Dark Mode is enabled, system-provided backgrounds (e.g., `Color(.systemGroupedBackground)` used in WeatherDashboardView) will turn dark, but custom text colors like `barnText` (near-black) will have poor contrast against dark backgrounds. 3. The `.parchment` card backgrounds remain cream/light regardless of system appearance. 4. The GuidedTourOverlay tooltip card uses `Color.parchment` background -- this remains light in Dark Mode, which may look inconsistent but is readable. 5. Some `.secondary` and `.tertiary` text colors (system-adaptive) will invert correctly, but they will appear on light custom backgrounds, which may reduce contrast. 6. For Apple review: note that the app is designed for light mode only. A Dark Mode adaptation is a future enhancement. The app should not crash or become completely unusable in Dark Mode, but visual inconsistencies are expected. |

## 3.5 In-App Purchase

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-019 | Paywall | PaywallView displays correct pricing, feature list, Restore Purchases button, and legal disclaimer | 1. App installed and onboarding completed. 2. `barnModeUnlocked` is `false` (default). 3. Exactly one horse exists in the database (to trigger the paywall gate). | 1. Open the Feed Board. 2. Tap the "+" toolbar button (accessibilityLabel: "Add horse"). 3. Since `!horses.isEmpty && !barnModeUnlocked` evaluates to `true`, the PaywallView sheet should present. 4. Observe the PaywallView layout from top to bottom. 5. Verify the dismiss button ("xmark.circle.fill") exists in the top-right toolbar. 6. Verify the crown icon (crown.fill) is displayed in saddleBrown color. 7. Read the title text. 8. Read the subtitle text. 9. Verify the feature list shows exactly 5 feature rows with correct icons and labels. 10. Verify the Subscribe button text and styling. 11. Verify the "Restore Purchases" button exists. 12. Read the legal disclaimer text at the bottom. 13. Tap the dismiss button (xmark.circle.fill) and verify the paywall closes without any state change. | 1. The PaywallView is presented as a sheet (`.sheet(isPresented: $showPaywall)`). 2. The dismiss button renders as "xmark.circle.fill" in `.secondary` foreground style in the `.topBarTrailing` toolbar position. 3. The crown icon displays at 60pt font size in `Color.saddleBrown`. 4. The title reads exactly "Barn Mode" in `EquineFont.largeTitle` and `Color.barnText`. 5. The subtitle reads exactly "Manage your entire barn." in `EquineFont.body` and `.secondary` color. 6. The feature list contains exactly 5 rows in order: (a) "Unlimited horses" with icon "horse.circle.fill", (b) "Full health tracking & analytics" with icon "heart.text.clipboard.fill", (c) "PDF owner reports" with icon "doc.richtext.fill", (d) "Blanketing recommendations" with icon "cloud.sun.fill", (e) "Maintenance reminders" with icon "bell.badge.fill". All icons are in `Color.hunterGreen` and text in `Color.barnText` using `EquineFont.body`. 7. The Subscribe button reads exactly "Subscribe -- $19.99/month" (EquineFont.headline) with "Cancel anytime" below it (EquineFont.caption, 0.8 opacity). The button has `Color.hunterGreen` background, `.white` foreground, and a 14pt continuous corner radius. 8. The "Restore Purchases" button renders in `EquineFont.caption` with `.secondary` foreground style. 9. The legal disclaimer reads exactly: "Payment will be charged to your Apple ID account. Subscription auto-renews monthly unless cancelled at least 24 hours before the end of the current period." in `.caption2` font, `.tertiary` color, center-aligned. 10. Tapping the dismiss button closes the sheet and `barnModeUnlocked` remains `false`. |
| AR-020 | IAP Persistence | Simulated purchase via @AppStorage persists barnModeUnlocked across app restart | 1. App installed with onboarding completed. 2. `barnModeUnlocked` is `false`. 3. One horse exists in the database. | 1. Open the Feed Board. 2. Tap the "+" button to trigger the paywall. 3. On the PaywallView, tap the "Subscribe -- $19.99/month" button. 4. Observe that the PaywallView dismisses automatically (the button handler sets `barnModeUnlocked = true` then calls `dismiss()`). 5. Verify the Feed Board now shows the "+" button behavior has changed: tapping "+" should now open AddHorseView directly (not the paywall). 6. Navigate to Settings and verify the subscription section shows "Barn Mode Active" with a checkmark.seal.fill icon in pastureGreen, and subtitle "Unlimited horses and full features." 7. Force quit the app completely. 8. Relaunch the app. 9. Navigate to Settings and check the subscription status. 10. Tap the "+" button on the Feed Board. | 1. After tapping "Subscribe", `barnModeUnlocked` is set to `true` via `@AppStorage("barnModeUnlocked")`, which writes to `UserDefaults.standard`. 2. The PaywallView dismisses via `dismiss()`. 3. On the Feed Board, the paywall gate condition `!horses.isEmpty && !barnModeUnlocked` now evaluates to `false`, so tapping "+" opens `AddHorseView` directly. 4. In Settings, the subscription section displays "Barn Mode Active" (not "Free Plan"), the icon is `checkmark.seal.fill` in `Color.pastureGreen`, and the subtitle is "Unlimited horses and full features." The "Upgrade to Barn Mode" button is hidden because `barnModeUnlocked == true`. 5. After force quit and relaunch, `@AppStorage("barnModeUnlocked")` reads `true` from UserDefaults. 6. Settings still shows "Barn Mode Active". 7. Tapping "+" on the Feed Board opens AddHorseView (no paywall). 8. Note: This is a SIMULATED purchase. There is no real StoreKit integration. The "Restore Purchases" button is a no-op (empty closure). |
| AR-021 | IAP Functionality | Unlocked Barn Mode allows adding unlimited horses beyond the free-tier limit of 1 | 1. App installed with onboarding completed. 2. `barnModeUnlocked` set to `true` (either via the simulated purchase or by manually setting UserDefaults). 3. One horse already exists. | 1. Open the Feed Board and verify the existing horse is displayed. 2. Tap the "+" toolbar button. 3. Verify AddHorseView opens (not PaywallView). 4. Fill in horse name "Thunder" and owner name "Test Owner". 5. Tap Save. 6. Verify "Thunder" appears in the Feed Board list. 7. Repeat steps 2-6 to add a third horse named "Lightning". 8. Repeat steps 2-6 to add a fourth horse named "Storm". 9. Add a fifth horse named "Breeze". 10. Verify all 5 horses appear in the Feed Board sorted alphabetically by name. | 1. With `barnModeUnlocked == true`, the paywall gate `!horses.isEmpty && !barnModeUnlocked` always evaluates to `false`, so the "+" button always opens `AddHorseView`. 2. Each horse is successfully saved via `modelContext.insert(horse)` with a success toast ("[name] added!"). 3. After adding all 5 horses, the Feed Board `@Query(sort: \Horse.name)` displays them in alphabetical order: Breeze, Lightning, Storm, Thunder, and the original horse. 4. The feed progress header shows "0/5 fed" (or equivalent count). 5. Each horse can be individually toggled as fed. 6. The Health tab shows filter chips for "All Horses" plus individual horse names. 7. The Weather tab shows blanketing recommendations for all 5 horses. 8. There is no upper limit enforced on horse count -- unlimited horses are allowed with Barn Mode unlocked. |
| AR-022 | Free Tier | Free tier provides complete functionality for 1 horse with no feature restrictions beyond the horse count limit | 1. App installed with onboarding completed. 2. `barnModeUnlocked` is `false`. 3. No horses exist yet (fresh post-onboarding state). | 1. Open the Feed Board. Verify the empty state shows: horse icon (64pt, hunterGreen 0.4 opacity), "No Horses Yet" title, "Add your first horse to get started with the digital feed board." description, and "Add Horse" button. 2. Tap "Add Horse" (or the "+" toolbar button -- both should open AddHorseView since `horses.isEmpty` is true, making the gate `!horses.isEmpty && !barnModeUnlocked` false). 3. Add a horse named "Bella" with owner "Free User", a photo, clipped status ON, full AM/PM feed schedule, and special instructions "Soak hay only". 4. Tap Save. Verify "Bella" appears in the Feed Board. 5. Toggle Bella as fed. Verify the "Marked as fed. Tap to undo." accessibility label reads out. 6. Navigate to the Health tab. Tap "+" to add a health event (Farrier) for Bella with cost $150. Verify the event appears in the timeline. 7. Navigate to the Weather tab. Verify Bella appears in the blanketing recommendations with "Clipped" badge. 8. Navigate to Settings. Verify subscription shows "Free Plan" with "1 horse included. Upgrade for unlimited." 9. Return to the Feed Board. Tap the "+" button. 10. Verify PaywallView appears (the gate triggers because `!horses.isEmpty && !barnModeUnlocked` is now true). 11. Dismiss the PaywallView. Verify the existing horse and all data are still intact. | 1. The empty state renders correctly with the "Add Horse" button (PrimaryButtonStyle). 2. Adding the first horse succeeds because the gate condition permits it when `horses.isEmpty`. 3. The horse appears in the Feed Board with all feed details: grain, hay, supplements, medications, special instructions (including the saddleBrown exclamation triangle alert box). 4. Feed toggle works: the circle changes from "circle" to "checkmark.circle.fill" in pastureGreen, the "Fed" StatusBadge appears, the fed timestamp shows, and the progress header updates to "1/1 fed". The "All AM Feeds Complete" / "All PM Feeds Complete" celebration banner appears. 5. Health event creation, editing, and display work identically to the paid tier for 1 horse. The event appears with the correct type icon, due status, and overdue formatting. 6. Weather blanketing recommendations work for the 1 horse, showing the correct recommendation based on clip status and temperature. 7. In Settings, "Free Plan" is displayed with the "Upgrade to Barn Mode" button showing "$19.99/mo" price. 8. Attempting to add a second horse triggers the PaywallView instead of AddHorseView. 9. All features (feed tracking, health timeline, weather, settings, notifications, templates) are fully functional for the 1 horse. The only restriction is the inability to add additional horses. |

## 3.6 Accessibility

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-023 | VoiceOver | VoiceOver navigation of Feed Board elements including feed toggle, avatars, badges, and undo banner | 1. VoiceOver enabled: Settings > Accessibility > VoiceOver > ON. 2. App installed with onboarding and guided tour completed. 3. At least 2 horses exist with feed schedules. One horse should be marked as fed and one unfed. | 1. Open the app. VoiceOver focuses on the first accessible element. 2. Swipe right to navigate through the Feed Board elements. 3. Navigate to the navigation bar. Verify VoiceOver reads the slot indicator and the "Add horse" button. 4. Navigate to the filter Picker ("All", "Needs Feeding", "Fed"). Verify each segment is announced. 5. Navigate to the progress header section. 6. Navigate to a horse row for an unfed horse. Observe what VoiceOver announces for the horse avatar, name, feed details, quick log button, and fed toggle. 7. Activate (double-tap) the fed toggle for the unfed horse. 8. After toggle, navigate to the undo banner that appears at the bottom. 9. Navigate to the same horse row again and verify the VoiceOver announcement changed. 10. Navigate to a row for an already-fed horse and verify its toggle announcement. | 1. The slot indicator reads: "Current feeding slot: AM" (or "PM"), using the explicit `.accessibilityLabel("Current feeding slot: \(viewModel.currentSlot.rawValue)")`. 2. The "+" toolbar button reads: "Add horse" (explicit `.accessibilityLabel("Add horse")`). 3. The filter Picker segments announce "All", "Needs Feeding", "Fed" with standard Picker accessibility. 4. For each horse row in `FeedBoardRow`: (a) The `horseAvatar` (`HorseAvatarView`) is announced as hidden (`.accessibilityHidden(true)`) and is skipped by VoiceOver. (b) The horse name, feed details, and badges are read as combined text within the NavigationLink. (c) The quick log button reads: "Log health event for [horse name]" (explicit `.accessibilityLabel("Log health event for \(horse.name)")`). (d) For an unfed horse, the fed toggle reads: "Not yet fed. Tap to mark as fed." (explicit `.accessibilityLabel`). (e) For a fed horse, the fed toggle reads: "Marked as fed. Tap to undo." (explicit `.accessibilityLabel`). 5. After double-tapping the toggle on an unfed horse, VoiceOver announces the state change. The undo banner appears with text "[horse name] marked as fed" and an "Undo" button. 6. The celebration banner (if all horses are fed) has a dismiss button with `.accessibilityLabel("Dismiss")`. 7. No decorative elements interrupt the VoiceOver navigation flow. |
| AR-024 | VoiceOver | VoiceOver navigation of Health Timeline including filter chips, event rows, and overdue indicators | 1. VoiceOver enabled. 2. App installed with at least 2 horses. 3. Multiple health events exist: at least one overdue event and one upcoming event across different types (Farrier, Vet). | 1. Navigate to the Health tab by swiping or tapping. 2. Swipe right to navigate through the Health Timeline elements. 3. Navigate to the event type filter chips row ("All", "Farrier", "Vet", "Deworming", "Dental"). 4. Focus on the "All" filter chip. Verify VoiceOver announcement. 5. Focus on a non-selected filter chip (e.g., "Farrier"). Verify announcement. 6. Double-tap to select the "Farrier" filter. Navigate back to the chip and verify the `.isSelected` trait is now announced. 7. Navigate to the horse filter chips row ("All Horses", individual horse names). Verify announcements. 8. Navigate to an overdue health event row. 9. Navigate to an upcoming (non-overdue) health event row. 10. Double-tap an event row and verify the edit sheet opens. | 1. Event type filter chips: (a) Each chip has `.accessibilityLabel("Filter by [title]")` -- so "All" reads "Filter by All", "Farrier" reads "Filter by Farrier", etc. (b) The currently selected chip has the `.isSelected` trait added via `.accessibilityAddTraits(isSelected ? .isSelected : [])`. VoiceOver announces "Selected" for the active filter. (c) Non-selected chips do not have the `.isSelected` trait. 2. Horse filter chips: (a) Each chip has `.accessibilityLabel("Filter by [title]")` -- "All Horses" reads "Filter by All Horses", horse names read "Filter by [horse name]". (b) Selected horse chip has `.isSelected` trait. 3. Health event rows (`HealthEventRow`): (a) Each row uses `.accessibilityElement(children: .combine)` to merge all child elements into a single announcement. (b) The combined `.accessibilityLabel` reads: For overdue events: "Overdue: [event type] for [horse name]. Overdue by [N] days". For upcoming events: "[event type] for [horse name]. Due in [N] days". (c) Each row has `.accessibilityHint("Tap to edit this event")`. (d) The event type icon and the chevron.right icon are both `.accessibilityHidden(true)` and are not announced separately. (e) The overdue exclamation mark icon is `.accessibilityHidden(true)`. 4. Double-tapping an event row opens the `AddHealthEventView` sheet for editing, which is announced by VoiceOver as a new screen/modal. |
| AR-025 | VoiceOver | VoiceOver navigation of Weather Dashboard including temperature, humidity, wind speed, and blanket recommendations | 1. VoiceOver enabled. 2. App installed with at least 1 horse. 3. Location permission granted and weather data loaded. | 1. Navigate to the Weather tab. 2. Swipe right to navigate through the Weather Dashboard elements. 3. Focus on the current weather display area (temperature, condition, icon). 4. Verify VoiceOver reads the combined weather information. 5. Navigate to the humidity label. 6. Navigate to the wind speed label. 7. Navigate past the "Updated X ago" timestamp. 8. Navigate to the "Blanketing Recommendations" section title. 9. Navigate to a blanket recommendation card for a horse. 10. If the horse is clipped, verify the clip status is included. | 1. The current weather display uses `.accessibilityElement(children: .combine)` to merge the weather icon, temperature, and condition into a single announcement. The `.accessibilityLabel` reads: "Current temperature [N] degrees Fahrenheit, [condition description]" (e.g., "Current temperature 45 degrees Fahrenheit, partly cloudy"). The weather condition SF Symbol icon is `.accessibilityHidden(true)`. 2. The humidity label has an explicit `.accessibilityLabel("Humidity [N] percent")` (e.g., "Humidity 65 percent") -- using the word "percent" instead of the "%" symbol. 3. The wind speed label has an explicit `.accessibilityLabel("Wind speed [N] miles per hour")` (e.g., "Wind speed 12 miles per hour") -- using "miles per hour" instead of "mph". 4. The "Updated X ago" timestamp reads using the relative date style. 5. The "Blanketing Recommendations" section title reads as a heading. 6. Each blanket recommendation card uses `.accessibilityElement(children: .combine)` to merge all elements. The `.accessibilityLabel` reads: "[horse name], [clipped/unclipped]. Recommendation: [recommendation]. [description]" (e.g., "Bella, clipped. Recommendation: Medium Weight Blanket. A medium blanket provides warmth without overheating."). The blanket icon circle is `.accessibilityHidden(true)`. |
| AR-026 | VoiceOver | VoiceOver navigation of Onboarding flow including progress indicator, step content, and navigation buttons | 1. VoiceOver enabled. 2. App freshly installed or onboarding reset (via `OnboardingManager.resetOnboarding()`). | 1. Launch the app. The OnboardingView should appear on the welcome step. 2. Focus on the progress indicator (capsule bar at top). 3. Swipe right through the welcome step content (icon, title, description, value props). 4. Navigate to the "Skip" button. 5. Navigate to the "Next" button. 6. Double-tap "Next" to advance to the barn setup step. 7. Navigate to the horse count selection cards (e.g., "1-3 horses"). 8. On the use case step, navigate to the PrimaryUseCase cards. 9. On the experience level step, navigate to the level options. 10. Continue through features and quickStart steps. 11. Focus on the progress indicator at each step. | 1. The progress indicator uses `.accessibilityElement(children: .ignore)` to suppress individual capsule announcements. Instead, it provides a combined `.accessibilityLabel("Step [N] of [total]")` and `.accessibilityValue("[X]% complete")`. For step 1 (welcome): "Step 1 of 6, 17% complete". For step 3 (useCase): "Step 3 of 6, 50% complete". For step 6 (quickStart): "Step 6 of 6, 100% complete". 2. The "Skip" button has `.accessibilityLabel("Skip tutorial")` and `.accessibilityHint("Skip onboarding and go directly to the app")`. 3. The "Next" button has `.accessibilityLabel("Next step")` and `.accessibilityHint("Continue to next step")`. On the final step, it reads `.accessibilityLabel("Get Started")` and `.accessibilityHint("Complete onboarding and start using the app")`. 4. The "Back" button has `.accessibilityLabel("Go back")` and `.accessibilityHint("Return to previous step")`. 5. Horse count `SelectableCard` elements have `.accessibilityLabel("[title]")` (e.g., "1-3 horses"), `.accessibilityHint("Selected" or "Double tap to select")`, and the `.isSelected` trait for the currently selected option. 6. `UseCaseCard` elements have `.accessibilityLabel("[displayName]. [description]")` (e.g., "Personal Horses. Managing my own horses"), `.accessibilityHint("Selected" or "Double tap to select")`, and the `.isSelected` trait. 7. Experience level options have `.accessibilityAddTraits(selectedExperience == level ? .isSelected : [])` for the selected level. |

## 3.7 Data Integrity and Edge Scenarios

| Test ID | Category | Test Description | Prerequisites | Steps to Execute | Expected Result |
|---------|----------|------------------|---------------|------------------|-----------------|
| AR-027 | Data Integrity | Timezone change correctly resets feed status when date boundary is crossed | 1. App installed with onboarding completed. 2. At least 2 horses exist with feed schedules. 3. Both horses marked as fed in the current time slot (AM or PM) with `amFedAt` / `pmFedAt` timestamps set to today. | 1. Open the Feed Board and verify both horses show as fed (checkmark.circle.fill in pastureGreen, "Fed" badge, progress shows "2/2 fed"). 2. Close the app (background, not force quit). 3. Open iOS Settings > General > Date & Time. 4. Disable "Set Automatically". 5. Manually advance the date by 1 day (e.g., from Feb 26 to Feb 27). 6. Return to the EquineLog app. 7. The app comes to foreground, triggering `scenePhase == .active`. 8. Observe the Feed Board state. 9. Pull down to refresh the Feed Board (triggers `viewModel.autoResetIfNewDay(horses:)`). 10. Verify the feed status for both horses. | 1. When the app returns to foreground or the Feed Board appears, `autoResetIfNewDay(horses:)` is called (in `.onAppear` and `.refreshable`). 2. The method iterates each horse's `FeedSchedule` and checks `Calendar.current.isDateInToday(amFedAt)` and `Calendar.current.isDateInToday(pmFedAt)`. Since the date was advanced by 1 day, the stored `amFedAt`/`pmFedAt` timestamps from "yesterday" return `false` for `isDateInToday`. 3. The method calls `schedule.resetDailyStatus()`, which sets `amFedToday = false`, `amFedAt = nil`, `pmFedToday = false`, `pmFedAt = nil`. 4. Both horses now appear as unfed: fed toggle shows "circle" icon in fenceLine color, no "Fed" badge, progress shows "0/2 fed". 5. The `currentSlot` (computed from `FeedSlot.current`) recalculates based on the current hour, which is unaffected by the date change (it depends on the time of day). 6. No crash or data corruption occurs. All horse data (name, feed schedule details, health events) remains intact. Only the daily fed status is reset. 7. NOTE: If the timezone changes without crossing a date boundary (e.g., traveling west by a few hours within the same calendar day), the feed status should NOT reset because `isDateInToday` would still return `true`. |
| AR-028 | Performance | Large data volume (20+ horses, 100+ health events) performs acceptably with @Query sorting | 1. App installed with `barnModeUnlocked = true`. 2. 25 horses added, each with a full feed schedule, a photo, and 4-5 health events (totaling ~100-125 health events). This can be set up programmatically or via repeated manual entry. | 1. Open the Feed Board. Measure the time from tab appearance to full list render. 2. Scroll through all 25 horses. Observe scroll performance (aim for 60fps, no visible jank). 3. Use the search bar ("Search horses...") to type a horse name. Verify filter responsiveness. 4. Tap the "Needs Feeding" filter chip. Verify filter applies within 1 second. 5. Navigate to the Health tab. Measure time to load the timeline with 100+ events. 6. Tap each event type filter chip ("Farrier", "Vet", etc.) and observe filter responsiveness. 7. Select a specific horse from the horse filter chips and verify only that horse's events display. 8. Navigate to the Weather tab. Verify all 25 blanket recommendation cards render in the ScrollView. 9. Navigate to a horse profile and verify the health event list and analytics load. 10. Delete a horse via swipe action and verify the cascade delete removes the horse and all associated health events and feed schedule. | 1. The Feed Board uses `@Query(sort: \Horse.name)` which generates an efficient SQLite ORDER BY clause. With 25 horses, the list should render within 1-2 seconds. 2. Scrolling should be smooth because: `HorseAvatarView` loads 50pt images from `@Attribute(.externalStorage)` data, and `FeedBoardRow` uses lightweight text views. 3. Search filtering via `filteredHorses(_:)` uses `localizedCaseInsensitiveContains` on in-memory Swift arrays -- for 25 horses, this is effectively instant. 4. The "Needs Feeding" / "Fed" filter iterates the 25-horse array checking `amFedToday`/`pmFedToday` boolean flags -- negligible computation. 5. The Health Timeline uses `HealthTimelineViewModel.upcomingEvents(from:)` which processes `horses.flatMap(\.healthEvents)` and groups/sorts by date. With 100+ events, this should complete within 1 second. 6. Event type filter chips filter the already-computed event groups by `HealthEventType` -- instant. 7. Horse filter chips filter by horse identity -- instant. 8. The Weather tab's `ForEach(horses)` renders 25 blanket recommendation cards in a VStack inside a ScrollView. Each card computes `BlanketRecommendation.recommend(temperatureF:isClipped:)` -- a simple conditional -- so rendering should be fast. 9. Cascade delete: `Horse` has `@Relationship(deleteRule: .cascade, inverse: \HealthEvent.horse)` for `healthEvents` and `@Relationship(deleteRule: .cascade, inverse: \FeedSchedule.horse)` for `feedSchedule`. Deleting 1 horse removes the horse, its feed schedule, and all its health events (~4-5 events) from SwiftData. The Feed Board and Health Timeline both re-query and update their lists. 10. After deletion, the Feed Board shows 24 horses and the Health Timeline shows correspondingly fewer events. |
| AR-029 | Data Integrity | Concurrent rapid feed toggle operations do not corrupt data or cause race conditions | 1. App installed with 5+ horses, all unfed in the current slot. 2. VoiceOver disabled (to allow fast tapping). | 1. Open the Feed Board with all horses visible. 2. Rapidly tap the fed toggle (circle) for 5 different horses in quick succession (within 2-3 seconds). 3. Observe the visual state of each horse row after tapping. 4. Check the progress header count (e.g., "5/5 fed"). 5. Scroll through the list to verify all 5 horses show as fed (checkmark.circle.fill, "Fed" badge). 6. Pull down to refresh the Feed Board. 7. Navigate away to the Health tab and back to Feed Board. 8. Verify the fed state is consistent after navigation. 9. Now rapidly tap the same 5 toggles again to unfeed them. 10. Verify all 5 show as unfed and the progress resets to "0/5 fed". | 1. Each `toggleFed(for:allHorses:)` call operates on a specific `Horse.feedSchedule` SwiftData model object, toggling its `amFedToday`/`pmFedToday` boolean and setting `amFedAt`/`pmFedAt` timestamp. 2. Since all operations occur on `@MainActor` (SwiftUI view and `@Observable` ViewModel), there are no concurrent writes -- each tap is processed sequentially on the main thread. 3. The undo banner tracks only the LAST toggled horse (`lastToggledHorse`). Rapid toggling causes earlier undo states to be overwritten, which is expected behavior -- only the most recent toggle can be undone. 4. The `undoTimer?.cancel()` call at the start of each `toggleFed` cancels any pending auto-hide of the undo banner, preventing stale undo state. 5. The `allFedCelebration` flag is checked after each toggle via `allHorses.allSatisfy { isFed(horse: $0) }`. If all 5 are fed after the last tap, the celebration banner appears exactly once. 6. The progress header's `fedCount(from:)` re-computes by filtering `horses.filter { isFed(horse: $0) }.count` on each re-render -- this always reflects the current state. 7. After pull-to-refresh, `autoResetIfNewDay` checks timestamps; since all `amFedAt`/`pmFedAt` are set to `.now` (today), `isDateInToday` returns true and no reset occurs. 8. All 5 horses persist their fed state through navigation because SwiftData auto-saves model changes. 9. Rapid unfed toggles work symmetrically -- each toggle flips the boolean and clears the timestamp. 10. No duplicate entries, no inconsistent state between visual display and underlying model. |
| AR-030 | Data Integrity | Template seeding is idempotent and does not create duplicate templates on repeated launches | 1. App installed with onboarding completed. 2. Default templates have been seeded (5 templates: "Hay Only", "Grain + Hay (Standard)", "Light Work", "Performance", "Senior"). | 1. Open the app and navigate to the Feed Board. 2. Tap "+" to open AddHorseView (with Barn Mode unlocked, or as first horse). 3. In AddHorseView, tap "Apply Feed Template" to open FeedTemplateLibraryView. 4. Count the number of templates displayed. Record their names. 5. Close the template library and cancel AddHorseView. 6. Force quit the app. 7. Relaunch the app. 8. Repeat steps 2-4: open AddHorseView and FeedTemplateLibraryView. 9. Count the templates again and compare to the first count. 10. Repeat the force-quit and relaunch cycle 2 more times, checking template count each time. 11. Verify each template's content matches the expected defaults. | 1. On first launch after onboarding, `seedDefaultTemplatesIfNeeded()` in `ContentView` checks `hasSeededDefaultTemplates` (`@AppStorage("hasSeededDefaultTemplates")`). If `false`, it inserts 5 `FeedTemplate` objects into the `modelContext` and sets `hasSeededDefaultTemplates = true`. 2. The 5 default templates are: (a) "Hay Only" -- description: "Basic hay diet, no grain", AM: no grain / 2 flakes Timothy, PM: no grain / 2 flakes Timothy. (b) "Grain + Hay (Standard)" -- description: "Common daily ration for an average adult horse", AM: 2 qt SafeChoice / 2 flakes Timothy, PM: same. (c) "Light Work" -- description: "Reduced grain for easy keepers or light riding", AM: 1 qt SafeChoice / 2 flakes Mixed, PM: no grain / 2 flakes Mixed, instructions: "Monitor weight weekly". (d) "Performance" -- description: "Higher calorie diet for horses in heavy work", AM: 3 qt Ultium / 3 flakes Orchard/Alfalfa mix, PM: same, instructions: "Electrolytes in water bucket after exercise". (e) "Senior" -- description: "Easily digestible ration for older horses", AM: 2 qt Senior feed / 2 flakes soft Timothy, PM: same, instructions: "Soak grain if needed. Monitor teeth." 3. On subsequent launches, `seedDefaultTemplatesIfNeeded()` checks `hasSeededDefaultTemplates`, which is now `true` in UserDefaults. The `guard !hasSeededDefaultTemplates` condition causes an early return, and NO additional templates are inserted. 4. The template count remains exactly 5 (plus any user-created templates) across all relaunches. 5. The `guard` also checks `onboardingManager.hasCompletedOnboarding`, ensuring templates are never seeded before onboarding completes. 6. If a user manually resets onboarding via `resetOnboarding()`, the `hasSeededDefaultTemplates` flag is NOT reset (it is a separate `@AppStorage` key), so templates are never re-seeded even after onboarding replay. |

---

## Test Environment Requirements

| Requirement | Details |
|-------------|---------|
| **Devices** | iPhone SE 3rd gen (smallest, 375pt), iPhone 15 Pro (standard), iPhone 16 Pro Max (largest), iPad Pro 12.9" (sidebar layout) |
| **iOS Version** | iOS 17.0+ (SwiftData requirement) |
| **Network** | Test with Wi-Fi, Cellular, and Airplane Mode |
| **Location** | Real device with GPS for WeatherKit (simulators may use simulated location) |
| **Time-Sensitive** | AM slot tests: run before 2:00 PM local time. PM slot tests: run after 2:00 PM local time. |
| **Fresh Install** | Some tests require clearing app data or reinstalling (onboarding, permission tests). |
| **Accessibility** | VoiceOver: Settings > Accessibility > VoiceOver > ON. Dynamic Type: Settings > Display & Brightness > Text Size. |
| **Dark Mode** | Settings > Display & Brightness > Dark (for AR-018). |

---

## Apple Review Rejection Risk Areas

| Priority | Risk Area | Description | Mitigation |
|----------|-----------|-------------|------------|
| **CRITICAL** | Simulated IAP | PaywallView uses `@AppStorage("barnModeUnlocked")` instead of real StoreKit 2. Apple will reject immediately. | Before submission: integrate StoreKit 2 with real product IDs, receipt validation, and functional Restore Purchases. Re-verify AR-019 through AR-022. |
| **HIGH** | Privacy Manifest | Ensure `NSLocationWhenInUseUsageDescription` and `NSPhotoLibraryUsageDescription` are in Info.plist with meaningful, specific descriptions. Apple rejects apps with missing or generic permission strings. | Verify Info.plist entries match the app's actual usage rationale. |
| **HIGH** | WeatherKit Entitlement | Requires an active Apple Developer Program membership and WeatherKit capability in the App ID. Missing entitlement causes runtime WeatherKit failure. | Verify the WeatherKit capability is enabled in the App ID and provisioning profile. |
| **MEDIUM** | Background Task Registration | `NotificationScheduler` uses BGTaskScheduler with ID `com.equinelog.notificationEvaluation`. This identifier must be in Info.plist under `BGTaskSchedulerPermittedIdentifiers`. | Verify the background task identifier is registered in Info.plist. |
| **LOW** | No Dark Mode | Hardcoded light-mode colors in `EquineTheme.swift` may cause readability issues in Dark Mode. Not a rejection cause, but Apple prefers apps that respect system appearance. | Document as known limitation. Consider adding Dark Mode support in a future update. |

---

*End of Test Plan*
